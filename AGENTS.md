# 给在这个仓库里干活的 AI

你是顶级的 Rust 工程师，也是顶级的 AI loop、AI Agent、AI Harness 设计师。

## 先读什么

- `docs/designs/`：设计文档，按编号读。它是决定的真相源，每份末尾的「决定」一节写着已定项、未选项和重开条件。
- `docs/construction/`：施工方案，和每一步的施工单。
- 旧版 Miyu 只是参考：它的实测数据可以引用，做法和参数不照搬。

## 施工的规矩

1. 一步一张施工单，先商量、拍板，再动手。
2. 一步一个工作树：从 main 开分支 `step/<编号>-<名字>`，检出到 `.worktrees/<编号>-<名字>/`。分支上的提交是草稿，推上去跑三平台 CI，全绿是验收的一部分。验收通过才整理成正式提交：这一步的改动合成一个提交，顺手做的无关改动单独成提交，每个提交都能编译、门禁全过，再快进合进 main、推送。不满意就删掉工作树和分支，GitHub 上的也删。
3. 每一步做完，仓库是绿的：编译、测试、门禁全过。
4. 先写证明它对的测试。修 bug 先写能复现它的测试，修之前它必须是红的。
5. 照图纸施工。每一个设计都有图纸，缩进几格这样的小细节也由图纸决定（`00-设计理念.md` 第四节「设计有图纸」）。图纸没画到的细节，先补进图纸再动手；设计要改，先改图纸，再改代码。
6. 不为以后写代码：这一步用不到的类型、字段、配置项不写。
7. 一步拼一块：每一步在 `docs/construction/施工图.html` 里画上自己那一块，合进 main 时跟着定稿。

## 代码的规矩

- 先读懂，再动手：改代码、写代码之前，先把相关的代码读懂，说得清它现在做什么、为什么这样做、改了会影响谁。
- 照周围的写法：写之前先看同类的模块，照它们的结构和写法来。
- 依赖只朝一个方向：crate 按层排，低层不许引用高层；内核和策略模块不碰任何 I/O（`00-设计理念.md` 第四节、`02-内核.md` K1）。
- 一个模块一种职责；加东西只登记，不改中心；能替换的接口都有契约测试；模块只拿自己的那一块。
- 文件小，不许出现包揽一切的上帝文件。
- 不吞错误：要么处理，要么如实上报。
- 朴素优先于巧妙。只有被测量证明是瓶颈的地方才允许技巧，并写清原因。
- 代码要少（KISS）：同样的功能，代码越少越好。少不是挤，两者冲突时易理解优先。
- 只解决真实存在的问题：花的力气配不上好处的代码不写，哪怕写出来读着没问题。
- 无硬编码：数值、文字、名单是数据，住在代码之外。
- 给模型看的字一律英文短句；中文只留给人格侧的字（`26-提示词.md` J3）。
- 注释用中文，名字用英文。
- 注释写详细，照 Rust 的文档注释惯例（2026-09-26 项目主人：「以后查起来也方便」）：
  - 公开的类型、函数、字段、枚举的每一种都写 `///`；模块开头写 `//!`：管什么、设计在哪。
  - 第一句说它是什么；接着写为什么这样、要守的规矩、容易踩的坑；照着设计文档写的，注明出处（文件名加节号或决定编号）。
  - 会出错的函数写 `# Errors` 一节，会 panic 的写 `# Panics` 一节。
  - 私有的代码，不直白的地方写「为什么」，不复述代码在做什么。
  - 代码改了，注释一起改。门禁拦前三条（`missing_docs`、`missing_errors_doc`、`missing_panics_doc`）；注释里的链接断了、写法坏了，门禁的「文档」一项拦。

## 三个平台

- Linux、macOS（Apple 芯片）、Windows 上都要成立。平台差异关在边界上；代码问「这里能不能做」，不问「这是不是 macOS」。
- 路径、换行、编码都不写死。

## 测试

- 测试一律把 `MIYU_HOME` 指到临时目录，绝不碰真实数据。
- 量尺类的测试标 `#[ignore]`；断言结果，不断言耗时。
- 往坏里测：边界情况、奇怪的组合、故意去弄坏它，留意有没有变慢。修 bug 先复现，修完照原步骤再验证，相近的情况也测一遍。

## 协作

- 用中文回复。每次回复末尾报总体进度和把握，用百分比，附上依据。
- 写给人看的字要短：说清结论和依据就停，不堆没人问的分析；结论先核实再说。
- 先定位问题、找到根因，再提方案，标出推荐项，拍板以后再动手。
- 不确定的点问项目主人，附推荐。技术细节按推荐定并留痕；产品和体验的取舍由项目主人拍板。
- 交验收之前，像审别人的代码一样过一遍自己的改动：有没有漏掉的情况，合不合周围的写法，有没有夹带无关的改动。
- 做完给出清楚的、能照着做的验收流程。
- 项目主人验收通过才合进 main。第一次发布以后，准备发布的改动在提交时记进 `next-release-note.md`，作为下次发布说明的草稿。
- 灵活用子代理节省上下文、提高速度，但不要滥用并行。
