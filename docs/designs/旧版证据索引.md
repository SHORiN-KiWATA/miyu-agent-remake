# 旧版证据索引

范围：`miyu-agent-remake/docs/designs/` 的 00 到 22 与 README，按设计文档编号排。整理期间这个目录一直有别的会话在改（2026-09-25 23:28 到 23:58 之间除 14、22 外的文件都动过，还新出了 23、24、25），所以位置一律写小节或决定编号，不写行号。本表按 23:57:52 最后一次改动之后的版本核对，2026-09-26 00:28 复查时没有新改动。README 只有约定，没有引用证据。23、25 不在原定范围里，但引了旧版证据，附在表尾；24 没有引。

旧仓库：作者本机的检出，本地 HEAD `8c18c5d5`。公开仓库是 GitHub 上的 SHORiN-KiWATA/miyu-agent，本机记录的 origin/main 停在 `04a23ccb`（2026-09-24），本地 main 比它多 76 个提交（没有联网核对）。

读法：

- 出处里的路径都相对旧仓库根目录，行号按本地 HEAD `8c18c5d5`。公开仓库落后 76 个提交，个别行号可能对不上。
- 缩写：〔P〕= `crates/miyu-hosts/src/platforms/`，〔RC〕= `crates/miyu-hosts/src/platforms/plugins/real_context/`，〔B〕= `crates/miyu-base/src/`，〔T〕= `crates/miyu-hosts/src/render/stream/timeline/`；「wiki 13」= `docs/wiki/13-QQ与通讯平台.md`；「管线文档」= `docs/qq-reply-pipeline.md`。
- 「提交 xxxxxxxx（09-24）」指旧仓库的 git 提交，日期都是 2026 年，出处是提交说明（`git show -s <sha>`）。`sha:路径:行` 指那个提交里的文件（`git show <sha>:路径`），用于已删掉的文件。没推送的提交和文件，列在「统计」一节末尾。
- 作者本机的 `MiyuPlan/` 计划目录不在仓库里，也不公开。
- 「本机 auto-memory」指作者本机 Claude Code 的 auto-memory 笔记，不在任何仓库里，读者看不到，所以不算出处。只有它有记载的，出处栏写「未找到出处」，备注里写笔记记了什么。
- `docs/wiki/` 几乎每页开头都有一句「临时版本提示：本文档由 DeepSeek 根据当前 Miyu 源码逐行整理……可能与最新代码存在出入」。只靠 wiki 的条目，备注里注明。
- 备注以「找到」「部分」「未找到」开头。「部分」是只找到了旁证、机制或其中一半。「对不上 #n」指表后清单的第 n 条。
- 标「外部参考」的行说的是 Claude Code 的机制，不是旧版 Miyu 的证据；列进来是因为它们的出处记在旧仓库里。

| 设计文档位置（文件与小节或决定编号） | 引用的说法（简短） | 旧版出处（路径:行，或小节） | 采集条件（模型、供应商、版本或日期、样本量） | 备注 |
|---|---|---|---|---|
| 00 §四「为什么」 | 旧版最常见的一类 bug 根因：同一件事在两三处各写一遍，改一处漏一处 | 例证：`docs/理念.md:274-278`（§九第 3 条：「补完主循环漏了子代理，补完落库漏了回合内的活体路径」）；`crates/miyu-core/src/llm/mod.rs:212-214`；`crates/miyu-core/src/slash_commands.rs:3-5`；提交 a70c9ebd（09-23）；提交 c9313cb0（09-17），它移植了 af95161e（「收缩那几步的构造从两份合成一份，顺手补上面板丢的尾巴」）与 770bc476（「去掉『同一件事写了几遍』的三处」，说明里有「改一处忘一处的经典形状」） | 定性说法，没有采集 | 部分：例子都在，「最常见」没有任何统计出处。af95161e、770bc476 只在本地标签 backup/render-golden-before-rebase 上 |
| 00 §四「依赖与接口的规矩」（09-25 补） | 一位读者按 SOLID 评过旧版：单一职责 D、开闭 C-、里氏替换 D、接口隔离 D、依赖倒置 F | 未找到出处 | — | 未找到。旧仓库与 MiyuPlan 都没有。新仓库 `docs/reviews/2026-09-25-第一轮审阅回应.md:11` 记它来自审阅者 E 的截图，截图不在两个仓库里 |
| 00 §四「依赖与接口的规矩」第 2 条 | 旧版病根之一：一个 25 个字段的回合上下文什么都装 | 同 18 §一 表第 3 行（〔P〕`turn_context.rs:54-89`） | 同 18 | 找到（引 18） |
| 00 §六「为什么」 | 同一配置值在运行时、终端设置页、网页设置页各一份；运行时改读新位置，两个设置页还绑旧字段，显示「未配置」 | 提交 a70c9ebd（09-23） | 09-23 真机；修复验证 `testkit/tui/kb_embedding_row.py` 22/22（旧二进制 7/22）、`testkit/settings-ui/kb_embedding_link.py` 12/12 | 找到。原文：那一行绑的是「08-10 embedding 提成全局之前的旧字段 `plugins.knowledge_base.embedding_*`」 |
| 01 §五 第 2 条 | 终端和网页各拼一套提示词，跨端切换一次，前缀缓存失效一次 | 现场核实：`crates/miyu-engine/src/agent/prompt.rs:106-155`（按 PromptAudience 拼系统提示词，终端这支比网页多 LaTeX 说明和语音协议）；终端回合 Owner（`crates/miyu-hosts/src/web/ipc_server.rs:787`），网页回合 External（`crates/miyu-hosts/src/web/turns/mod.rs:172`） | 没有实测记录 | 部分：代码能证明两端系统提示词不同；「切换就掉缓存」的明文只在本机 auto-memory `miyu-audience-prefix-split.md:8`，笔记还说这是安全边界、不是 bug |
| 01 D1 未选 A | 旧版每个头自己算显示，同一条显示规则在 TUI、网页各写一遍 | 提交 a2bb3b4a（09-24，网页 `app.js` 抄一份工具名单，靠单测 `web_fold_summary_lists_match_rust` 对照）；`docs/plan/2026-09-17-render-unification.md`（施工进度表：「StepKind 三处合一」「收缩行点开的样子（三处 → 一处）」） | 不适用 | 找到。终端内部就有好几份；TUI 与网页之间因此出事的具体记录没找到 |
| 01 D2 未选 | 旧版教训：单次命令退出时，会把它拉起的后台任务一起杀掉 | `AGENTS.md:70`；提交 717bace6（08-15） | 不适用 | 找到。对不上 #1 |
| 02 K2「代价」 | 旧版为了保住缓存本来就把注入内容留档（化石化） | `docs/理念.md:94-111`（§三「删除的代价：化石化的由来」）；`AGENTS.md:30`；提交 6599404b（08-06）第 2 条 | DeepSeek 官方与 opencodego（mimo），字节级抓包、curl 重放；连续两轮请求共享 84% 字节，分叉点在上轮 prompt 末尾 | 找到 |
| 04 P4 未选 | 每个头自己解析命令，正是旧版「一条规则写三遍」的老问题 | `crates/miyu-core/src/slash_commands.rs:1-6`；提交 cb28acd5（08-18）；QQ 另有解析器 〔P〕`commands.rs:98` | 不适用 | 找到。对不上 #2 |
| 05 §九；I6 未选 | 工具从目录里消失，被模型理解成「能力被拿走了」，旧版出过 | 提交 db101d05（09-01） | claude-code 中转线；09-01 群内转录；全库 530 条带 miyu 工具的转录里 33 条出现过清单移除，最早 08-29 | 找到。对不上 #3。另见提交 62418a08（09-25，WIP）：QQ 两件工具只在 NapCat 连上时注册，重连就换工具表 |
| 05 I8 | 旧版因为启动时等 MCP 服务器，整个程序卡住过 | 提交 d6b24808（09-04，标题带 #36） | 3 个死 server：修前 8018ms 被 CLI 的 8 秒就绪窗口杀掉，修后 3192ms 就绪；无 MCP 基线 269ms | 找到。#36 是 GitHub issue 号，没联网看原文 |
| 06 U4 未选 | 旧版自己实现的 PBKDF2 | `crates/miyu-core/src/state/accounts.rs:3-14`；提交 fe0b50b8（09-10） | 不适用 | 找到。PBKDF2-HMAC-SHA256，HMAC 自己写，60,000 轮 |
| 06 U8 | 项目主人确认：旧版里在 QQ 私聊让她跑命令天天在用 | 同 18 Q7 未选那一行 | 口述 | 部分（引 18） |
| 07 §二 | 旧版出过测试误用真实数据目录的事故 | 未找到出处 | — | 未找到。仓库里只有规则：`AGENTS.md:70`、`docs/wiki/14-参与开发.md:123`。本机 auto-memory `miyu-memory-poisoning-by-bugs.md:26` 记有：08-30 `testkit/fake-onebot` 跑的是真 daemon、真人格库，造了 19 条假 episode 混进真实记忆 |
| 07 §六 | 同一进程里打开又关闭活库，丢了 SQLite 依赖的文件锁，把库弄坏过 | `AGENTS.md:56`（3.3）；提交 278ae076（08-22） | 08-21 本机 conversation.db 损坏；原文「机理已在副本复现」 | 找到。对不上 #4 |
| 07 §八；S2 未选 A | 旧版最脆弱的是数据库迁移和按列序读取的查询；活库不能直接复制 | `AGENTS.md:54`（3.1「全库最脆弱处」）、`:56`；`docs/plan/2026-09-09-compact-v3.md:52` | 不适用 | 找到。对不上 #5 |
| 07 §十 | 旧版出过两个核心同时运行、各以为自己是唯一的 | 提交 01d18f90（09-21）；后续 85f3e511（09-21） | 09-21 本机：两个 daemon（pid 3642418、3922473）跑在同一份数据上，调研期间又冒出第三个 | 找到。原文另注：WAL 加 busy_timeout 下多进程写是安全的，真问题是白占资源（36.5MB + 43.5MB、0.80% CPU）和两份内存态 |
| 07 S9 未选 | JSONC 是旧版的配置格式 | `docs/wiki/05-配置指南.md:9`；`docs/wiki/11-数据与目录.md:13`；〔B〕`shell/mod.rs:843` | 不适用 | 找到 |
| 08 §一 第 1 条的说明；C1 未选 | 旧版最常见的一类缓存事故：实时请求边跑边拼、回放时从存档重建，对不上就断前缀（案卷 `2026-09-24-缓存回放与活体对不上`） | `docs/fixed/2026-09-24-缓存回放与活体对不上.md`「根因：回放不是原样重放」（:20-36）；同类另见 `docs/plan/2026-09-23-cache-forensics.md` §四（:126-165）与案卷「追记一」（:120-135） | 案卷：09-23 01:37 到 09-24 15:00，opencodego 聊天请求 2.41 亿 prompt token；验证用官方 deepseek-flash（`replay_live.py`，每轮三次工具调用），长会话重型版每边 47 次请求、约 785 万 prompt token | 找到。对不上 #6。案卷文件只在本地 main |
| 08 §三 tools（C4）；C4 未选 | 旧版按说话的人切换工具面，工具被移除被读成掉线，此后整段会话不调工具 | 提交 db101d05（09-01） | 同 05 §九 | 找到。对不上 #3 |
| 08 §三 C5 | 约束解码的模型面对空壳参数只能发 `{}`，契约写在对话里也救不回 | 提交 ac1bb8de（09-01） | bigmodel glm-5.3-flash；裸 API 8/8 复现；隔离 MIYU_HOME 直连 E2E：全量时参数正常，存根时 `{}`；失败时补契约的补救在场也照样 `{}` | 找到 |
| 08 §三 system | 人格的示范对话放在 system 之后，旧版实测它对保持人格很有效 | 提交 71e48dc7（08-14，「预设对话…实测闲聊中位 63→48 字」）；提交 189a3909（08-16，「A/B 实证干净体制预设对话已足够」） | 71e48dc7：人格遵循度 A/B 约 230 发生产模型请求，按人格显式规则机械打分；模型原文没写；A/B 原始结果在 `testkit/persona-ab/results/`，被 .gitignore 排除 | 部分。对不上 #7 |
| 08 §四 C9；C9 未选 | 旧版落盘时才截断，下一轮断前缀，命中 76.3%，对照组 87.6% | `docs/plan/2026-09-23-cache-forensics.md` §四（:126-165，表在 :137-140）；提交 08286954（09-23） | `testkit/cache-forensics/prune_ab.sh`；真模型 mimo-v2.6-flash（opencodego）；两组独立沙盒 daemon 交替跑同一串提示词，水位推到 0.99；每组 16 次请求、8 个新轮；A 组 6 次前缀改写，落点全是 tool | 找到。对不上 #8 |
| 08 §五 | 旧版群聊的回复对象机制：过期的「这一轮回复谁」被回放，造成跨轮错乱 | `AGENTS.md:32`（1.4）；提交 bfaedf35（08-23）；提交 eb6d8ef3（08-13） | bfaedf35 引 08-23 日志 | 找到。bfaedf35：定向指令「曾内联在本轮消息块里，因随化石重放造成跨轮语义错乱而被删」，改成 system 常量（现 〔P〕`onebot/turn.rs:627`） |
| 08 §五 | 浮动尾巴只损失它自己：旧版实测尾部提醒的缓存命中约 99.6% | 提交 71e48dc7（08-14，「跨回合仅损失自身 ~80 tok 前缀缓存，实测命中率仍 99.6%」） | 同一批 A/B；模型、上下文长度原文没写 | 找到。对不上 #9。本机 auto-memory `miyu-persona-adherence-experiment.md:21` 补了细节：opencodego 上的 deepseek-v4-flash，约 31.8k token 的真实群聊会话，31,744/31,858 |
| 08 §五 C10 | 人所在终端的信息只能取头报的：旧版实测过，核心进程里读到的终端类型和 shell 和人真正用的对不上，既错又占字数 | `crates/miyu-engine/src/agent/prompt.rs:203-209`（「08-17 实测」：TERM、SHELL 是 daemon 启动时冻结的值，「实测 daemon=xterm-kitty 而客户端=tmux-256color」，三个字段「既是错的又占 91 字符」）；引入提交 a7428c69（08-17） | 08-17 本机 | 找到 |
| 08 §五 C10；C10 已定 | 环境类事实变了才注入；时间按粒度写（终端到小时、场所到分钟）、带时区、星期三个字母；关掉的补一条「已关」：旧版一直这么做 | 提交 189a3909（08-16，「终端面时间降到小时级；拼装时与历史里最近一份 runtime 化石逐字节比对，相同即跳注入…平台面保留分钟级」）；`crates/miyu-engine/src/agent/prompt.rs:210-229`（时区写成「UTC+09:00」的理由，e5dbf57d，08-26）；`:178-181`（沙盒「说过而现在关了，补一条关了」，1069b7d0，09-23）；`crates/miyu-engine/src/agent/context.rs:25-35`；案卷「追记二」（:137-161） | 189a3909：opencodego 上的 deepseek-v4-flash，8/8 请求纯追加 | 找到 |
| 08 §五 C2；C2 未选 | 把当前消息从群聊记录块之前挪到之后，措辞不改，跨轮持续指令遵循率 80%→100%（N=32，p=0.00012）；注入放在触发消息后是旧版布局 | 提交 eb6d8ef3（08-13）；〔RC〕`inject.rs:754-757`；旧布局：`AGENTS.md:30`、`docs/理念.md:82-92`、提交 6599404b 第 1-2 条 | deepseek-v4-flash；整批真实 API 1120 次调用；该场景 N=32，原文没说是每组还是合计；简单场景有 97% 天花板；六场景复测 94.8%→99.0%（p=0.036） | 找到。对不上 #10 |
| 08 §五 C7 | 行为禁令式的措辞没有作用，位置比措辞重要 | 提交 eb6d8ef3（08-13）；〔RC〕`inject.rs:834-837` | 同上；单独改写 `<qq-identity-policy>` 尾句 p=0.83 | 找到。对不上 #11 |
| 08 §五 C7「模板与转义怎么写」（09-26 补） | 旧版把不可信字段先照 JSON 字符串转义，再把 `&` `<` `>` 换成 `\u0026` `\u003c` `\u003e`；群聊记录一行一条，换行因此伪造不了记录行 | 〔RC〕`targeting.rs:660-682`（`safe_prompt_string`、`safe_prompt_field`，旧仓库本地 HEAD `1fa02004`）；用处〔RC〕`history.rs:210-246`（一行一条：`[时间] 发送者 [msg=编号]: 正文`） | 代码现场核实，没找到专门的实测记录 | 找到。引号转成 `\"`，放进标签属性里还带着引号；重制版改成 `\u0022` |
| 08 §六 | 旧版 `理念.md` 第六章把供应商分三类：契约型、尽力而为型、按次计费型 | `docs/理念.md:178-192`（§六「供应商不是抽象接口」） | 2026-08-06 实测批次（理念.md:3-4） | 找到。原文契约型还列了 Kimi；「写入付费」举的是 Anthropic 写入 1.25x～2x |
| 08 §六 保温 | 在 DeepSeek 上空闲 65 秒后跨轮命中 28%→94.9%；在别的供应商上零收益、白花请求 | `docs/理念.md:199-206`；提交 6599404b（08-06）第 3 条；`docs/wiki/10-系统设计与架构.md:104` | DeepSeek 官方，同链对照，空闲 65 秒，每 20 秒 ping 一次、max_tokens=1、非流式；对照组是 opencodego 上的 mimo：97.2% vs 97.3%；样本量原文没写 | 找到。对不上 #12 |
| 08 §六 写入宽限 | 尽力而为型的缓存写入是异步的，上一请求刚结束就发会错过缓存 | `docs/理念.md:161`；`:208-210`；提交 6599404b 第 3 条 | DeepSeek 官方：1.5 秒内跟进会 miss，6 秒后稳定；宽限阈值 2.5 秒 | 找到（设计没说「旧版」，但事实取自这里） |
| 08 §六 钉住端点 | 旧版的钉住端点是一个全局开关 | `docs/cache-and-prompt-plan.md:119`（计划中的 `llm.cache_sticky` 开关，默认关）、`:216`（列在「规模门控（后续批次）」）、`:34`、`:170` | 08-06 计划文档 | 部分。对不上 #13 |
| 08 §七 核心指标 | 旧版教训：百分比会说谎，一轮加入大段新内容时命中率下跌，看着像缓存坏了 | `docs/理念.md:235-237`（§八）；案卷 :104-105（「衡量口径（09-24 与用户对齐）」）；`docs/plan/2026-09-23-cache-forensics.md` §七（:204-218） | cache-forensics §七：09-22，全部 opencodego，纯聊天能到 99%，调工具、长输出掉到 70–90% | 找到 |
| 08 §七 测试门禁 | 旧版回放测具的尾巴全是空的，漏掉了一次回归 | 案卷「追记一」（:120-135，原话 :127-129） | 09-24 晚做 C3 时实测抓到；`crates/miyu-engine/src/agent/tests/replay_prefix.rs` 新增三条带尾巴用例，修前全红 | 找到。案卷只在本地 main |
| 09 §八 | 旧版教训：快模型加短会话恰好绕开了超时的问题 | `docs/compact-plan.md:217` | 压缩 A/B 测具：deepseek-v4-flash、25 轮会话，输出 3394–6083 token，耗时 21.9–36.0 秒，离 90 秒超时线很远；实况撞上的是 148k 上下文配 opus（`:200`） | 找到 |
| 09 开头；§二至§六多处 | （外部参考）压缩机制取自本机 Claude Code 2.1.263 | `docs/plan/2026-09-09-compact-v3.md:1`、`:14-24` | 09-09；原文「全部从本机二进制抠出，非推测」 | 找到。细节摘录另在本机 auto-memory `claude-code-compact-internals.md` |
| 09 §二；Z3 | （外部参考）输出预留与 20000 取小；有效窗口减 13000 | `docs/compact-plan.md:33` | Claude Code 2.1.263 | 找到。原文还有「−33000 警告、−3000 硬阻断」 |
| 09 §四 压后重建 | （外部参考）文件最多 5 个、单个 5000、合计 50000 token；技能单个 5000、合计 25000 | `docs/compact-plan.md:36`；`docs/plan/2026-09-09-compact-v3.md:20` | 同上 | 找到 |
| 09 §四「检查点的包装」（09-26 补） | 旧版的检查点是一条 user 消息，`<conversation-checkpoint>` 外壳，写明摘要是历史、不是新的指令；没有「接着做」那句 | `crates/miyu-engine/src/agent/tool_report.rs:326-344`（`summary_checkpoint_message`，旧仓库本地 HEAD `1fa02004`；注释写着：放进 system 的摘要，里面的祈使句会被当成新指令再做一遍）；「not as new instructions」那半句取自 opencode V2：`docs/compact-plan.md:118` | 代码现场核实 | 找到 |
| 09 §三、§四、§五 | （外部参考）九节摘要；先分析再摘要；用量 = 最近一次 API 用量 + 本地估算；附完整转录路径；被动压缩保留最后一组 | `docs/plan/2026-09-09-compact-v3.md:19-24`；`docs/compact-plan.md:199` | 同上 | 找到 |
| 09 §六；Z7、Z9 | （外部参考）摘要超长砍最老的 20%、最多 3 次；连续失败 3 次暂停；压完 3 回合内又满、连续 3 次就熔断 | 后两条：`docs/compact-plan.md:36`、`:126`；「砍最老 20%」未找到出处 | 同上 | 部分。对不上 #14 |
| 10 §二 表「压缩后的工作集」 | 旧版的实证：读文件全走 `cat`，压缩后要重新读回的文件恒为空 | `docs/compact-plan.md:212`（09-09 当日回归表） | 09-09 compact v3 上线当天的实况会话：折叠区 6 次 edit、0 次 read，`restored=[]` | 找到。对不上 #15 |
| 10 §三 B10；B10 未选 | 把一种能力藏进 `read` 的 `kb:` 前缀，模型常常想不起来用 | `AGENTS.md:47`（「kb: 前缀实测翻车史」）；提交 0fc813db（08-22） | 原文没写 | 部分：只有规则和提交，事故细节不在仓库。对不上 #16 |
| 10 §九 | 有的模型受声明的参数格式硬约束，空壳只能发空参数；旧版为此改成按模型选全量或懒加载，后来又删掉了混合方式 | 提交 ac1bb8de（09-01）；后续 e6830141（09-01，默认改全量）、a3747078（09-01） | 同 08 C5 | 找到。对不上 #17 |
| 12 §一 R5 | 旧版出过「按名字结束进程，结果把自己也结束了」 | `docs/plan/2026-09-23-cache-forensics.md:229`、`:242-246`；提交 290c03e9（09-23） | 测具脚本里的事 | 部分。对不上 #18 |
| 12 §三 R4 | 一条打错的子命令被当成对话，发给了正在运行的核心 | `docs/code-review-2026-08-16.md:232-236`（随 54cab5cf 入库，08-16）；`AGENTS.md:70`；机制：`src/cli/args.rs:45-46`、`src/cli/tests/cli_args.rs:438-440` | 08-16 代码审查的黑盒探测，一次，产生了一轮真实对话和 usage 记录 | 找到。对不上 #19。本机 auto-memory `miyu-live-testing-notes.md:107-108` 另记了 09-16 的一次（`miyu tool --help`） |
| 12 §八 R14 | 旧版发布过漏了资源文件的包 | 提交 2e72970e（08-21，「0.4.5 资产已按此重建替换」）；`AGENTS.md:83`（资产必须含 fonts/，`tar -tf` 验）；`miyu-release-workflow.md:105` | v0.4.5（4743cb94，08-21） | 部分：仓库只写了「重建替换」。「首版 22.6MB 缺字体，重建 46.5MB」只在本机 auto-memory `miyu-low-footprint-2026-08-21.md:13` |
| 12 R3 未选 | 每改一句提示词都要重新编译，旧版就是这样 | `AGENTS.md:82`（7.1）；`docs/wiki/05-配置指南.md:226` | 不适用 | 找到 |
| 13 §三 规则 1；H3 | 一步一行：图标、显示名、对象、用时；用时紧跟，不推到最右 | 〔T〕`mod.rs:709-718`、`:522-531`；`docs/releases/0.6.0/changelog.md:14` | 本地 HEAD 代码；v0.6.0（09-14） | 找到。对不上 #20 |
| 13 §三 规则 2 | 连接行 `│` 与图标同列、单独一行；正文前加 `│ ` | 〔T〕`mod.rs:66-68`、`:567-581`、`:81-88`；〔T〕`glyphs.rs:101-121`；〔T〕`live.rs:198-211` | 本地 HEAD 代码 | 找到。对不上 #21 |
| 13 §三 规则 3 | 说话就收起；收起 `›`、展开 `⌄`；进行中的不收 | `crates/miyu-hosts/src/render/stream/mod.rs:314`；〔T〕`mod.rs:359-374`；提交 ac54ef64（09-13） | 本地 HEAD 代码 | 找到 |
| 13 §三 规则 4 | 收缩行永远英文：`Ran N commands`、`Worked for`、`Thought for`；中间依次 edits、tools、thoughts、err | 〔T〕`live.rs:644-697`（:646-649 三种写法）；提交 a2bb3b4a（09-24，「用户看示例后拍板」） | a2bb3b4a：用例 2909→2912，tui/run.py 98/99，webui-timeline 22/24 | 找到。对不上 #22 |
| 13 §三 规则 5 | 进行中「思考中 · token 数 · 用时」，下面滚动最后几行；结束收成「已思考」加一句摘要 | 〔T〕`live.rs:601-627`；〔B〕`config/defaults.rs:153-155`（默认 10 行）；提交 2bc6711b（09-17）；结束那行 〔T〕`mod.rs:877-885`；`docs/plan/2026-09-17-render-unification.md:43` 与 §6.3 第 1 项 | 本地 HEAD 代码 | 找到。对不上 #23 |
| 13 §三 规则 6 | 失败整行红，连接行颜色不变 | 〔T〕`mod.rs:486-497`、`:567-569` | 本地 HEAD 代码 | 找到 |
| 13 §三 规则 7 | 用户消息前是洋红 `┃`；AI 正文平铺不加框 | `src/cli/footer.rs:90-95`；`src/cli/repl/layout.rs:123-131`；`docs/plan/2026-09-10-tui-rewrite.md:52` | 本地 HEAD 代码；施工单 09-10 | 找到。对不上 #24 |
| 13 §三 规则 8 | 后台任务结束、发生压缩、换了模型，都是一行铃铛通知 | 〔T〕`mod.rs:119-125`；`src/cli/repl/tail/queue.rs:201-211`；`src/cli/repl/remote/slash_context.rs:148-158`；`crates/miyu-hosts/src/render/agent_events.rs:80-85`；`crates/miyu-hosts/src/render/stream/mod.rs:402-416`；「换了模型」那条未找到出处 | 本地 HEAD 代码 | 部分。对不上 #25 |
| 13 §三 规则 9；§十 第 8 行 | 图标用 Nerd Font，每类工具一个；没有时只用 `$`、`?`、`⚙` | 〔T〕`glyphs.rs:7-24`；〔T〕`mod.rs:89-147`；`crates/miyu-hosts/src/render/style.rs:63-68` | 本地 HEAD 代码 | 找到。对不上 #26 |
| 13 §三 表 | 「说话就收起」这条规则，旧版在终端、网页、子代理三处各写了一份 | `docs/plan/2026-09-17-render-unification.md` §2.1 第 4 行、§1.2 表；`web/app.js:530-531`、`:978-981`；终端三个触发点 `crates/miyu-hosts/src/render/stream/mod.rs:314`、〔T〕`subagent.rs:550-553`、`src/cli/repl/tail/screen/overlay/log.rs:146、320`；提交 c9313cb0（09-17） | 调研按 32227cc0 | 找到。对不上 #27 |
| 13 §七 首段；H7 | 后台任务逐条列在底栏下方，「旧版是这样」 | `src/cli/repl/jobs.rs:17-19`、`:50`、`:61-63`、`:80-89`；`src/cli/repl/tail/frame.rs:448-462` | 本地 HEAD 代码 | 找到。对不上 #28 |
| 13 §七「停止」 | 旧版的回合循环不看停止标记，AI 说话时按 `x` 停不掉 | 提交 69eb4d1f（09-24）；`src/cli/repl/job_stop.rs:3-6`；提交 950aa898（09-24） | 950aa898：`testkit/tui/direct_overlay_keys.py` 修前红三条 | 找到。对不上 #29 |
| 13 §九「内存」 | 旧版全屏演示在 110×32 终端里放五条长回复，实测不到 4MB | `testkit/tui-demo/README.md:34-36`（「110×30：RSS 4.0 MB / 匿名 0.6 MB」）；`docs/plan/2026-09-10-tui-rewrite.md:17-18`（「demo 单独进程 RSS 3.8 MB」）；`testkit/tui-demo/drive.py:12-13`（默认 110×32）；提交 63afd18f（09-11） | 独立的 ratatui 演示程序，不接 daemon（README:3），回复是伪造的（README:57）；单次测量 | 部分：「110×32、五条长回复、3.4MB」只在本机 auto-memory `miyu-tui-rewrite-research-2026-09-10.md:11`。对不上 #30 |
| 13 §十 第 1 行 | 输入框上方的浮动内容不加左侧 `┃` | `docs/plan/2026-09-10-tui-rewrite.md:55`；`testkit/tui-demo/README.md:51`；`src/cli/repl/tail/screen/toast.rs:175-184` | 施工单；本地 HEAD 代码 | 找到（产品里的浮层是一圈细线框） |
| 13 §十 第 2 行 | 斜杠命令列表锚在输入框上方，只占 4 行，边打边筛，不写按键说明 | 演示 `testkit/tui-demo/src/main.rs:1356-1357`（`SLASH_ROWS = 4`）；产品 `src/cli/repl/commands.rs:107-123`（最多四条）、`src/cli/repl/tail/screen/toast.rs:125-129`、`crates/miyu-core/src/slash_commands.rs:388-402` | 本地 HEAD 代码 | 找到。对不上 #31 |
| 13 §十 第 3 行 | 粘贴与图片占位符 `[粘贴 1: ~120 行]`、`[Image 1: 截图.png]`，洋红，整块跳过、整块删除 | `src/cli/repl/placeholder.rs:6-16`、`:110-116`、`:413-431`；`src/cli/repl/editor.rs:480、492`；`src/cli/repl/input.rs:828-831`；提交 46889bb1（08-23） | 本地 HEAD 代码 | 找到。对不上 #32 |
| 13 §十 第 4 行 | 复制提示：输入框上方绿色细边框小盒子，2.5 秒后消失 | 演示 `testkit/tui-demo/src/main.rs:450`（2500 ms）、`:1331`；施工单 `docs/plan/2026-09-10-tui-rewrite.md:58`；产品 `src/cli/repl/tail/screen/toast.rs:29-33`（2200 ms）、`:98-103`；`docs/plan/2026-09-11-tui-implementation.md:1128-1129` | 演示 09-10；本地 HEAD 代码 | 找到（只有演示程序是这样）。对不上 #33 |
| 13 §十 第 5 行 | 空会话的开场：星空动画、标志、一句开场白 | `src/cli/repl/banner/mod.rs:1-10、160-189`；〔B〕`terminal/starfield.rs:1-14、85`；`docs/releases/0.6.0/release-notes.md:22`；「一句开场白」未找到出处 | 本地 HEAD 代码 | 部分。对不上 #34 |
| 13 §十 第 6 行；§六 Esc | `Esc` 由近及远一层层关，什么都没开才打断回合 | `src/cli/repl/tail/screen/tail_impl.rs:315-343`；`src/cli/repl/editor.rs:274-287`；`src/cli/repl/commands.rs:88` | 本地 HEAD 代码 | 找到。对不上 #35 |
| 13 §十 第 7 行 | 终端不在前台时，回合结束、等你确认都弹通知并响一声；在 herdr 里由 herdr 响 | `src/cli/repl/jobs.rs:120-167`；`src/cli/repl/remote/one_shot.rs:1100-1108`；`src/cli/repl/question_flow.rs:76-82`；提交 d9e66b85（09-18）、f7379843（09-23） | d9e66b85：niri + mako 1.11 + kitty 0.48 真机，notify_sound.py 18/18；f7379843：herdr 0.8.2，6/6 | 部分。对不上 #36 |
| 13 H1 已定 | 旧版从 2026-09-11 起就是全屏 | `docs/plan/2026-09-11-tui-implementation.md:3-6`；提交 e9bf333f（09-13）、9142225c（09-14）；v0.6.0（09-14） | git 历史 | 找到。对不上 #37 |
| 13 H1 未选 | 跟着 shell 滚动是旧版最早的形态；在上面反复出过残影和错位 | `docs/fixed/2026-08-17-终端图像与渲染.md` §八；`docs/fixed/2026-09-07-kitty图片滚动残影.md`；提交 8cb1e264（09-07）、98b2183f（08-20）、adf255da（09-10）；`docs/plan/2026-09-10-tui-rewrite.md:12、93` | 8cb1e264：无头 cage + kitty 0.48.2 + grim 像素判定，旧版 0.5.0-2 复现 15 行残影、修后 0 | 找到 |
| 13 H6 未选 | 旧版的后台任务行只能用鼠标点开 | `src/cli/repl/tail/screen/tail_impl.rs:239-243`、`:370-410`（打开面板的生产调用只有 :407 一处）；`docs/plan/2026-09-10-tui-rewrite.md:66` | 本地 HEAD 代码，全仓 grep 调用点 | 找到 |
| 13 H9 未选 | 旧版每秒拉一次后台任务表，新开终端或换会话时先闪出别的会话的任务，数字也慢一拍 | `src/cli/repl/jobs.rs:600-602`（7×150ms，一轮约 1 秒）；提交 950aa898（09-24）；「数字也慢一拍」未找到出处 | 950aa898：`other_session_jobs.py`，第二个终端 80 帧里 48 帧漏出别的会话的任务，/new 后 46 帧，修后 0 | 部分。对不上 #38 |
| 14 §二「为什么非这样不可」；G1 未选 A（21 §二、X4 未选同义） | 终端设置页手写表单、字段靠下标对应；网页字段表从终端的中文字符串抽出来，是第二份；embedding 挪到全局后，两个设置页都绑着旧字段，显示「未配置」 | ① `src/config_tui/settings.rs:40-189`（:140-148「The read-back below is by index…」）；② `web/settings-schema.js:1-4`；③ 提交 a70c9ebd（09-23） | a70c9ebd：见 00 §六 那一行 | 找到。对不上 #39 |
| 14 G5 第 3 条（22 §七「装」同义） | 旧版更新 shell 钩子时，替换文件把符号链接变成了普通文件 | 提交 99c5194e（09-19）；〔B〕`shell/mod.rs:643-647`、`:1001-1016` | 开发中的红测抓到；`testkit/shell-hook/run.py` 12/12，七条单测 | 找到。对不上 #40 |
| 14 G1 未选 B | 旧版网页最早按值的类型自动生成表单，对象和列表全变成大文本框 | 已删的计划 `cdde36da^:docs/plan/2026-09-04-settings-redesign.md:7-18`；提交 5d83427a（09-04） | 不适用 | 找到。原文「大片字段只能手写 JSON」「四个 textarea 手填 JSON 对象」「插件页是按 JSON 类型自动生成的…枚举字段是裸文本框」 |
| 14 G6 未选 | 旧版网页先改成草稿、最后点保存 | `cdde36da^:docs/plan/2026-09-04-settings-redesign.md:60`（保存栏：改动数、「放弃」「保存」）、`:146`（保留草稿/载入/保存） | 不适用 | 找到 |
| 15 §四 | 旧版那六个层层继承的模型槽 | `docs/wiki/13-QQ与通讯平台.md:40-51`（表在 :44-49） | 不适用 | 找到（只靠 wiki）。六行：平台文本池、平台多模态池、非白名单文本、回复判定、好感度、入群审批，缺省多是「继承」上一层 |
| 15 §五；M4 未选 | 旧版只有轮换，是为了把各家的免费 API 轮着用（项目主人 2026-09-25） | 轮换：`crates/miyu-core/src/llm/openai_compatible/endpoints.rs:60-66`、`:91-111`；`docs/wiki/05-配置指南.md:47`、`:197`；理由：项目主人口述 | 不适用 | 找到。对不上 #41 |
| 15 M5 | 旧版实测：固定冷却会让一直挂着的端点每两分钟被重新信任一次 | 提交 84051e11（08-25）；`crates/miyu-core/src/llm/openai_compatible/endpoints.rs:68-71` | 08-24 mimo 车道故障日；扁平 120s（瞬时档）/600s（限流档）冷却；每轮 30 秒以上挂死等待 | 找到。原文是故障日的观察，不是专门的测量 |
| 15 §六 M7；M7 未选 | 会话钉的模型没了，核心直接断开连接，界面只说「无效响应」 | 提交 15e0caf1（09-18）；更早的同类 88db3878（08-28） | 09-18 真机：会话钉着 opencodego/union-alpha，供应商清单里已没有 | 找到。原话是「invalid admin response」 |
| 15 §八 M9 | 项目主人在旧版说过：「如果价格不能精准计算的话我宁愿没有价格显示。」 | 未找到出处 | — | 未找到。只在本机 auto-memory `no-imprecise-money.md:9`（09-24，撤掉 footer 会话花费时说的）。仓库里相近的是提交 9c4496be（08-20）：用量查询工具不再返回金额估算，「models.dev 价目与实际计费对不齐」 |
| 15 M1 未选 | 旧版的全局文本池、多模态池、分级池、平台槽各成一套 | `docs/wiki/05-配置指南.md:35-36`、`:195-210`；`docs/wiki/13-QQ与通讯平台.md:40-51` | 不适用 | 找到（只靠 wiki） |
| 16 §一 | 旧版的普通模式和开发模式；开发模式单独有一套提示词 | 提交 d76ee8bf（08-15，normal/dev 双入口，dev 会话挂在保留人格 dev 名下）；提交 afafe349（09-24，去掉内置的「You are a helpful software engineer assistant.」）；`docs/wiki/05-配置指南.md:233` | 不适用 | 找到。对不上 #42 |
| 16 §三 Y8；Y8 未选 | 旧版终端里没有 QQ 的工具，她用 curl 去猜平台的接口，猜错了还下结论说平台不支持（issue #49） | 同 18 §九 那一行：未找到出处 | — | 未找到（引 18）。对不上 #56 |
| 16 §六 Y5 | 旧版的 `/dev` 就是这样：模式钉在会话上，要换就换一条会话 | 提交 9855d198（09-18） | 不适用 | 找到。原文「模式钉在会话上……『切到 dev』就是换到一条 dev 人格的会话」 |
| 16 §八 | 旧版 Miyu 的生活助理脚本 | 提交 0a120b38（09-01）；`src/personas/default/scripts/`（17 个） | 不适用 | 找到。0a120b38 原文列了「生活助理配件（小红书/闲鱼/酒店比价/B站直播/游戏破解/电池/进程/环境变量脚本 + Linux 排障技能）」 |
| 16 Y2 未选 | 技能和脚本跟着人格走，是旧版的做法 | 提交 0a120b38（09-01）；`AGENTS.md:45`（2.1.2） | 不适用 | 找到。对不上 #43 |
| 16 Y6 未选 | 开发预设不带人格，是旧版开发模式的做法 | 提交 afafe349（09-24）；提交 d76ee8bf（08-15） | 不适用 | 找到。对不上 #42 |
| 17 §三 L4；L4 已定 | 沿用旧版的日记、经历、知识点三类；后台整理，强度衰减，想起一次算复习 | `docs/wiki/08-记忆系统.md:7-35`（§1 到 §4） | 不适用 | 找到（只靠 wiki）。旧名是短期日记、长期日记、知识点 |
| 17 §四 | 旧版把召回结果放在消息后面 | `docs/wiki/08-记忆系统.md:29`（「联想结果只追加在本轮消息尾部…这是缓存设计的刻意选择」）；`docs/cache-and-prompt-plan.md:199`（A2+B1，08-06）；`AGENTS.md:30` | 不适用 | 找到。改到前面的理由见对不上 #10 |
| 17 §六；L7 未选 | 一个 bug 让她说了错话，错话被记成记忆；bug 修好以后错的记忆还在，得手动查库 | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-memory-poisoning-by-bugs.md:10-14`：08-30 私聊追问历史图片失败，她把这件事记成公共知识，bug 修好后照样不去试。那个 bug 的修复在仓库里：提交 f1996383（08-30） |
| 17 §八 | 旧版有五种重置命令，语义重叠，文档专门有一节提醒「别混」 | `docs/wiki/08-记忆系统.md:63-73`（「## 7. reset 的几种语义（别混）」） | 不适用 | 找到（只靠 wiki）。五条：`/reset`（`/clear`）、`/reset all`、`/reset-memory`、`/reset-all-memory`、`/wipe`；`/reset all` 和 `/reset-all-memory` 都清当前人格的全部记忆 |
| 18 §一「换平台只换桥」末句；Q1 未选 | 旧版这套逻辑写在 QQ 平台层的插件里，换平台就得重写；平台层自己又长出一套回合管理和插件系统 | 现场核实：〔RC〕（`docs/qq-reply-pipeline.md:6-8` 指向这里）；设置键 `platforms.qq.plugins.real_context`（〔RC〕`mod.rs:76-77`）；插件 trait 〔P〕`plugins/mod.rs:283` | real_context 里 `platforms.qq`、`ONEBOT_PLATFORM`、`onebot` 的非测试引用 29 处、7 个文件 | 部分：「换平台就得重写」是从代码推的，没有原文；wiki 13:5 的说法正相反：「设计上 OneBot 只是第一个平台，核心层是平台无关的」 |
| 18 §一「旧版乱在哪里」导语 | 子代理逐文件核对过旧版 3.9 万行的平台层 | 现场核实，数的是 〔P〕 | 2026-09-25，HEAD 8c18c5d5，命令 `find crates/miyu-hosts/src/platforms -name '*.rs' -not -path '*/tests/*' -not -name test_support.rs \| xargs cat \| wc -l`：88 个文件 38,695 行（非空 36,635）；再扣内联测试是 35,618；全部 141 个文件 51,444 行 | 找到（口径：不含测试目录，含空行）。「子代理逐文件核对过」本身没有书面记录 |
| 18 §一 表第 1 行 | 回合排队与抢占，大约有八套机制 | 现场核实，8 处：〔P〕`onebot/connection.rs:19、486`；〔P〕`mod.rs:100`；〔P〕`scheduling.rs`；〔P〕`turn_order.rs`；〔RC〕`runtime.rs:214、228`；〔RC〕`pending.rs`；抢占 〔P〕`plugins/mod.rs:316`、〔P〕`mod.rs:178`、〔P〕`turn_ownership.rs`；〔P〕`onebot/turn.rs:312` | 读各模块头注释，grep Semaphore、ticket、preempt | 找到。数法有弹性：算上 〔P〕`inflight.rs` 和 〔P〕`onebot/group_join.rs:29` 是 10 个 |
| 18 §一 表第 1 行 | 自己的插件系统，17 个挂接点 | 〔P〕`plugins/mod.rs:283-434`（`trait PlatformPlugin`） | 现场核实：18 个方法，去掉 descriptor 是 17 个 | 找到 |
| 18 §一 表第 1 行 | 自己的权限判断 | 〔P〕`access_control.rs`（117 行）；〔P〕`plugins/access_manager.rs`（770 行）；动态授权表 `crates/miyu-core/src/state/migrations/baseline.rs:427、445`；wiki 13 §4 | 现场核实 | 找到 |
| 18 §一 表第 1 行 | 自己的聊天记录库，还带迁移 | 〔P〕`plugins/message_history/store/schema.rs:7`（SCHEMA_VERSION = 4）、`:38`（migrate）；提交 eccc625d（08-04）；wiki 13 §8 | 现场核实 | 找到 |
| 18 §一 表第 1 行 | 判官、好感度、入群审批三处各自直连模型、各自重试 | 直连：〔RC〕`judge.rs:99`、〔RC〕`affection/mod.rs:559`、〔P〕`onebot/group_join.rs:285`；重试：〔RC〕`judge.rs:107`、〔P〕`onebot/group_join.rs:295`；好感度只有超时 〔RC〕`affection/mod.rs:580-594` | 现场核实 | 部分。对不上 #44 |
| 18 §一 表第 2 行 | 「是不是管理员或白名单」至少算了 7 处，其中一处漏看了动态授权 | 〔P〕`access_control.rs:74、86`；〔P〕`onebot/turn.rs:227-233`；〔P〕`onebot/admission.rs:213-296`（四处）；〔P〕`onebot/group_join.rs:50-62`；〔P〕`turn_context.rs:330-373`；〔P〕`plugins/access_manager.rs:410-417`；〔RC〕`inject.rs:82-95` | 现场核实：grep admin_users、whitelist、has_dynamic_access，7 个文件 12 处以上 | 找到。漏看动态授权的那处（〔RC〕`inject.rs:82-95` 只查静态私聊白名单）是核查时认定的，设计文档没点名 |
| 18 §一 表第 2 行 | 取出站文字的函数有三四份 | 〔P〕`reply.rs:226`；〔P〕`plugins/message_history/mod.rs:445`；〔RC〕`history.rs:332`；〔P〕`plugins/reply_processor/settings.rs:249` | 现场核实：生产代码 4 份 | 找到（内联测试里另有 2 份） |
| 18 §一 表第 2 行 | 长文转图的渲染器开了两个 | 〔P〕`render_tool.rs:28-30`；〔P〕`plugins/reply_processor/mod.rs:21、33`；各带 WorkerSlot 〔P〕`plugins/renderer/mod.rs:107-111、143-146` | 现场核实 | 找到。render_tool 是 ea5733d7（09-22）加的，它的注释（:25-26）却写「整个 daemon 共用一个渲染子进程」 |
| 18 §一 表第 3 行（00 §四 亦引） | 一个 25 个字段的回合上下文 | 〔P〕`turn_context.rs:54-89`（PlatformTurnContext） | 现场核实：25 个字段；文件 1,029 行 | 找到 |
| 18 §一 表第 3 行；Q14 未选 | 处理一条消息的函数约 720 行，判断要不要开口的约 590 行 | 〔P〕`onebot/dispatch.rs:128-845`（handle_message_with_activity）；〔RC〕`inject.rs:31-617`（decide_group_trigger） | 现场核实：718 行、587 行 | 找到。Q14 那句见对不上 #62 |
| 18 §一 表第 4 行 | 14 种 `<qq-*>` 提示词标签散在 11 个文件里 | 〔P〕`onebot/` 下 3 个文件（forward、identity、turn）；〔P〕`plugins/` 下 7 个；`crates/miyu-engine/src/agent/input.rs` | 现场核实：grep `</?qq-…`，排除测试：11 个文件、14 种；只数真拼进提示词的是 13 种、8 个文件；算上测试 16 种、17 个文件 | 找到。对不上 #45 |
| 18 §一 表第 4 行 | 最底层的公共代码里放着 QQ 头像地址和 QQ 专用端口 | 头像 〔B〕`platform_types.rs:583-617`（层序表「基础」层，`test_scripts/arch_dep_check.py:53-54`）；QQ 专用端口 trait 〔B〕`host_ports/ports.rs:42-162`（QqOutreachPort 等）；网络端口默认 8300 〔B〕`config/platform.rs:615、877` | 现场核实 | 找到（「端口」按接口 trait 或网络端口理解都成立） |
| 18 §一 表第 5 行；§六「这些状态都从日志里算出来」 | 一个群的状态散在好几处；冷静和续聊状态只在内存里，一重启就丢 | 历史库（上一行）；会话历史累积重放 d8089416（08-10）；插件键值表 〔RC〕`pending.rs:27-56`；核心表 `crates/miyu-core/src/state/migrations/baseline.rs:276、293、314`；内存 〔RC〕`runtime.rs:68-76` | 现场核实 | 找到。对不上 #46 |
| 18 §一 表第 6 行；Q11 未选 | 入口各走各的：消息排队、命令抢先、唤醒不排队、定时消息绕过插件；同一会话拼出两份系统提示词 | 排队 〔P〕`onebot/dispatch.rs:596-638`；命令抢先 `dispatch.rs:692-693`、提交 6f29a2f3；唤醒 〔P〕`onebot/turn.rs:101-183`（只拿全局许可 〔P〕`turn_run.rs:49-54`）；定时 〔P〕`plugins/scheduled_messages/mod.rs:1-6`、〔P〕`onebot/proactive.rs:37-88`；两份提示词：唤醒回合只有 `system_context`（turn.rs:161），普通回合另加 5 段常量（turn.rs:617-640） | 现场核实 | 找到。「两份系统提示词」是对照代码看出来的，没有书面记录 |
| 18 §三 要点；Q13 未选 | 白名单、按会话的专属配置、平台默认是三套各自的东西 | 〔B〕`config/platform.rs:933-943`、`:946-966`（「按会话 > 按会话类型 > QQ 默认」）、`:916-926`；动态授权 〔P〕`access_control.rs:59-83`；wiki 13 §1.5 | 现场核实 | 找到 |
| 18 §三 要点 | 旧版 QQ 这条线约 195 个配置项，「要不要开口」占 95 个，存成不带类型的 JSON，每轮重新解析 | 95：〔B〕`config/platform_plugins/real_context.rs:25`（RealContextPluginSettings 的字段数）；无类型 JSON 〔B〕`config/platform_plugins/mod.rs:65-70`；另见提交 5d83427a（09-04，「真实上下文 94 项」）、`cdde36da^:docs/plan/2026-09-04-settings-redesign.md:16`（「真实上下文 120 项」） | 现场核实（数结构体字段，不算 legacy 字段）：real_context 95，QQ 全部合计 208；`web/settings-schema.js` 平台页 44 加插件 130 = 174 | 部分：95 对得上字段总数，195 数不出来。对不上 #47、#48、#49 |
| 18 §四；Q2 未选 | 旧版另开一个聊天记录库，和会话各存一份；撤回、删除要在两处各做一遍 | 提交 eccc625d（08-04）、d8089416（08-10）、67942936（09-20）；删除工具只删历史库 〔P〕`plugins/message_history/tools/delete.rs` | 原文没写样本 | 部分：两份存储有出处，「两处各做一遍」是推出来的 |
| 18 §四；§七「撤回」 | 撤回的标记记下了，读的一侧没人看，撤回的消息在历史里凭空消失 | 提交 67942936（09-20） | 真实库 797 条撤回，795 条带标记，794 条有操作者；779 个测试；QQ 真机没跑 | 找到。修的只是历史工具那一侧，实时群聊上下文照旧不带撤回，这是有意的 |
| 18 §四；Q2 | 旧版在进门之前没有按消息编号去重 | 现场核实：〔P〕`onebot/` 分发侧没有去重；历史库 `INSERT OR IGNORE`（〔P〕`plugins/message_history/store/write.rs:86`）加 UNIQUE（schema.rs:200、225）；重连靠连接代数（〔P〕`onebot/connection.rs:21-24`） | grep dedup、idempot、duplicate | 找到（没有文档记过）。对不上 #50 |
| 18 §四 | 旧版文档说睡眠时段的消息不进历史，代码却在准入之前就存了 | 文档：wiki 13:120、提交 88c57875（09-10）；代码：〔P〕`onebot/connection.rs:579-606` → 〔P〕`plugins/message_history/mod.rs:297-304` 写库，早于 〔P〕`onebot/dispatch.rs:169` 的准入 | `testkit/qq-sleep/run.py` 的 11 项只验回不回（:162-191），没验历史 | 找到 |
| 18 §六「顶替」 | 后一条接过前一条的回合，旧版定为 7 秒 | 管线文档「覆盖窗口」节；〔B〕`config/platform_plugins/real_context.rs:178`（默认 7）；提交 39e3f799（09-19，「覆盖窗口 5 → 7 秒（用户拍板）」） | 原文没写样本 | 找到。09-19 之前是 5 秒 |
| 18 §六「触发条件」 | 旧版原来是一条 if-else 优先级的梯子，后来才改成集合 | 提交 39e3f799（09-19）；管线文档「触发条件」节 | real_context 87/87，platforms 406/406 | 找到。原文补了一句：判官那边一直是三份加分相加 |
| 18 §六「主动插话」「违规审核」 | 判官从五个方面打分，并说明是不是在跟她说话；违规命中时不管有没有人叫她都触发 | 〔RC〕`judge.rs:20`、`:460`；wiki 13:194-195；管线文档「违规判定成立之后」；〔B〕`config/platform_plugins/real_context.rs:208` | 原文没写样本 | 找到。「是不是在跟她说话」（to_bot）是 b92bcc7a（09-24）才加的 |
| 18 §六「冷静机制」；Q4 | 冷静只管插嘴；「最近说了多少」按半衰期回落；一两句几乎不抬，连说三四句才明显 | 提交 b92bcc7a（09-24）；〔RC〕`restraint.rs:9-36`；管线文档里的表 | d0830016（09-24）：7 天日志复算 6040 次判断，被拦下的抽 24 条、14 条是冲她来的；回放：冲她来 143→285/285，插嘴 47→42/128；`testkit/qq-reaction` 18/18；模型原文没写 | 找到。同一天反转过一次：d0830016 先定「被 @ 照样受压」，b92bcc7a 才改成只管插嘴。对不上 #51 |
| 18 §六 冷静机制子条 | 项目主人在旧版说过：「没有人回去调挡位的」 | 管线文档:100；〔B〕`config/platform_plugins/real_context.rs:55`；〔RC〕`restraint.rs:18`（三处都由 b92bcc7a 写入） | 用户 09-24 原话 | 找到 |
| 18 §六「两边各看各的」 | 程序的加减分写进判官提示词，判官就重复算分；分数一摆在模型面前，她就拿它立规矩 | 前半：提交 b92bcc7a（09-24）；后半：提交 28ca1390（09-09）、302fb77f，〔RC〕`affection/scoring.rs:20`，〔P〕`onebot/identity.rs:318-326` | b92bcc7a：13 条判断理由直接引用了加减分；302fb77f 记一天八次 | 找到。对不上 #52 |
| 18 §六 Q5 说明末条；Q5 未选 | 好几条路径硬起「必须说点什么」的回合，不说为什么叫她、不给不说话的路；「这没 @ 我，我先旁听」被回放后变成自我认知；旧版靠提示词叫她「不该回就别回」 | 硬起回合：提交 cb73b5e2（08-29）、b3b84112（08-31，「当天三条『没@我就先旁听』泄漏全由此强起」）；不说明：b0db9859（只对「概率抽中且判官放行」的回合注入一句）；说出口和回放：302fb77f（一天八次）、b0db9859（「会话历史里已经攒了十三条同样的话——她在照抄自己」）；「变成自我认知」「靠提示词叫她别回」未找到出处 | 302fb77f：假 OneBot 跑五轮；模型原文没写 | 部分。对不上 #53、#54。「自我认知」只在本机 auto-memory `miyu-meta-refusal-2026-08-29.md:16` |
| 18 §七「引用」 | 旧版一开始没写这条规矩，她把群友引用的别人的话当成对她说的 | 提交 add26ece（08-20，加 `<qq-reply-format>`）；〔P〕`onebot/turn.rs:621-623` 的注释（「用户 08-20 实测点名」） | 伪 NapCat 群聊真机问答 1 例；1634 个测试 | 找到 |
| 18 §八 出站链第 2 条；Q8 | 端点出故障那天，她把「发送」一遍遍重做、每次换说法；图片按哈希、文字按相似度去重，两道闸都不能拆 | `AGENTS.md:62`（4.3）；提交 384c87ff（08-22）、6f29a2f3（08-20）；〔P〕`turn_context.rs:80-83` | 384c87ff：08-22 三次复现，端点 ox-alpha 断流 94 次，相似度门槛 ≥0.66；6f29a2f3：真机同一张图连发 4 次；模型只写了「弱模型」 | 找到。旧版另有跨回合的短闸（文字 2 分钟、0.75；图片 5 分钟，〔P〕`activity.rs:18、34、104-121`），设计写「只在同一回合里生效」就去掉了它们 |
| 18 §八 出站链第 3 条 | 沿用旧版：隔了几条别人的消息才引用，隔了一会儿才 @ | 默认 〔B〕`config/platform_plugins/real_context.rs:202`（4 条）、`:204`（15 秒）；判定 〔P〕`reply.rs:57-79`；提交 e99dfcd8（08-30）；wiki 13:79 | 原文没写样本 | 找到。对不上 #55 |
| 18 §八 出站链之后第 1 条 | 旧版工具发的消息查两道去重，最终回复只查一道，而且不记账 | 工具 〔P〕`tool.rs:322`（查回合内与跨回合）、`:428`（登记）；最终回复 〔P〕`onebot/outbound.rs:306` 只查回合内 | 现场核实 | 找到。「不记账」指不进去重账本，历史库照记 |
| 18 §八「命令回执」；Q8 | 命令回执 3 秒后自动撤回 | 提交 6645f956（09-24）；〔P〕`commands.rs:79、84-91`；wiki 13:143-146 | `testkit/qq-reset-recall`：改前 4/11，改后 11/11（3.00 秒撤回） | 找到。撤的是 `/reset`、`/reset-memory`、`/reset-all-memory`、`/stop`；先定 5 秒，试后改 3 秒 |
| 18 §八「长文转图」 | 版式照旧版：长宽跟着内容定、尽量接近方形、续栏重复表头 | 提交 b3048739（09-24）；〔P〕`plugins/renderer/columns.rs:12-20、138、190`；wiki 13:74-78 | columns.py 新版 6/6、旧版 1/6 | 找到。设计没写单页 5000 像素、总 2000 万像素的上限 |
| 18 §九「哪些会话有这些工具」；Q16 未选 A | 旧版有意把平台工具从终端拿掉；终端里一概没有平台工具 | 未找到出处（「有意」）。代码现状：平台工具只在平台回合里注册（〔P〕`tool.rs:15-40`、〔P〕`live_turns.rs:1-11`）；终端里有 `send_qq_message`，普通模式还有 `qq_contacts`，NapCat 连上才注册（`crates/miyu-engine/src/tools/platform_outreach.rs:1-17`；提交 d08ee92f、ce26a643） | 现场核实 | 未找到。对不上 #56 |
| 18 §九；Q16 未选 A（16 Y8 同） | 旧版 issue #49：终端里只有两件 QQ 工具，她用 curl 去猜 NapCat 的接口，猜错拿到 404，还下结论说「OneBot 不支持读历史消息」 | 未找到出处 | — | 未找到。查过全部提交说明（含所有分支）、docs、crates、src、testkit、test_scripts、AGENTS.md、next-release-note.md、MiyuPlan；本机 auto-memory 里只有 `miyu-remake-project.md:36` 转述这份设计。旧仓库会留 issue 号（3ae891b9「Closes #29」），唯独没有 #49。相近的是 BUG-14：她在 QQ 会话里用 run_command 打 NapCat 的 HTTP 口发消息（`crates/miyu-engine/src/tools/platform_outreach.rs:16-17`） |
| 18 §十一 SnowLuma | 发出的消息编号一半是负数，收引用时又扔掉负数 → 自动引用一半静默失效，消息照发、回「成功」 | 提交 cfe3b764（09-19） | SnowLuma 1.14.7；真机 43 条引用：正数号 22 条都好，负数号 21 条全丢；对端日志当天 126 次报错；上游 1.14.9 修复，本机升 1.14.17 复验；quote_target.py 6/6 | 找到。对不上 #57 |
| 18 §十一 NapCat | 查自己的禁言状态用成员缓存 → 提前解禁以后她以为还被禁言，一个群连着两个多小时不说话 | 提交 db4e27c1（09-11）；〔P〕`onebot/adapter.rs:196-211`；〔P〕`onebot/caches.rs:47` | 09-11 一次：禁言 12 小时，22 分钟后提前解禁，她「一声不吭两小时二十分」；NapCat 版本原文没写 | 找到。对不上 #58 |
| 18 §十一 视频 | 视频消息的编号只有通用的取文件接口认 → 她看不了别人发的视频 | 〔P〕`onebot/adapter.rs:477-480`；提交 4d4f97df（09-04） | qq-video 五个场景真模型全过，模型原文没写 | 找到。对不上 #59 |
| 18 §十一 语音 | 语音要先按 wav 取回再识别 → 只显示成「语音消息」 | 〔P〕`onebot/voice_inbound.rs:1-8`；提交 0fd1b11c（09-06） | `testkit/qq-voice`；样本原文没写 | 找到。对不上 #60 |
| 18 §十一 加群 flag | 部分实现加群请求的 flag 格式不一样 → 处理加群时报「系统已处理」 | 〔P〕`onebot/group_join.rs:169-180`；提交 fece7ef0（08-16）；wiki 13:181 | 原文没写样本和版本 | 找到。对不上 #61 |
| 18 §十一 表下要点 | 旧版有一次花几十分钟读 NapCat 的源码，其实连着的是 SnowLuma | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-qq-quote-dropped-2026-09-19.md:18`；cfe3b764 的说明里没有这段 |
| 18 Q7 未选（§三「主人」；06 U8 同） | 主人在 QQ 私聊里让她跑命令，旧版天天在用 | 代码佐证：〔P〕`turn_context.rs:354-373`（管理员的 host_tools_allowed 直接为真）；〔P〕`mod.rs:217-224`（不许用宿主工具时才换成受限工具面，受限面里没有 run_command，`crates/miyu-core/src/llm/openai_compatible/zen_tools.rs:31`）；提交 3ae891b9 | 现场核实 | 部分：能做到有代码佐证，「天天在用」是口述，没有频率数据。旧版管理员在群里也有宿主工具，不分群聊私聊 |
| 18 Q12 未选 | 旧版一套写死的判断，只能用开关关掉其中几段 | 〔RC〕`inject.rs:31` 那个 587 行的函数；〔B〕`config/platform_plugins/real_context.rs` 里 25 个布尔开关 | 现场核实 | 找到 |
| 18 Q15 未选 | 旧版的最终回复、工具发的消息、定时消息是三种记法 | 最终回复写历史库（〔P〕`turn_context.rs:594` → 〔P〕`plugins/message_history/mod.rs:344-350`），不进去重账本；工具另在 〔P〕`tool.rs:428` 登记；定时消息「先记账再发送」记在任务循环的内存集合里（〔P〕`plugins/scheduled_messages/mod.rs:43、66-67`），直发不经 after_send（〔P〕`onebot/proactive.rs:37-88`） | 现场核实 | 找到。其实还有第四种：故障短通知（〔P〕`turn_context.rs:630-631`） |
| 19 §三；N3 未选 | 向量索引存成 JSON，136MB，每查一次解析 2.1 秒 | `docs/plan/2026-09-05-local-embedding-research.md:18`、`:69`、`:129` | 09-05；本机 Ryzen 9 7940H（16 线程，AVX-512）；本机真实语料；知识库语义索引 10548 条（bge-m3） | 找到。同样数据存 f32 BLOB 只要 43MB |
| 19 §三 | 成员点「重建索引」，起了不带成员身份的子进程，重建的是管理员的库 | 提交 e95cd6a3（09-11） | 隔离 daemon 真机验证（kb-member-reindex-probe.py） | 找到。原文：子进程建的是默认库（`~/.miyu/data/kb`），成员的进度停在 starting |
| 19 §四；N4 未选 | 旧版把知识库做成 `read` 的 `kb:` 前缀，实测她想不起来用 | 同 10 B10 那一行 | 同 10 B10 | 部分（引 10）。对不上 #16 |
| 19 §四 | 和网上查到的冲突时她先信知识库，旧版回答 Linux 问题就是这个规矩 | `e3129ffe^:src/prompts/miyu.md:49-51`（这条在 09-21 被提交 e3129ffe 删掉）；`docs/wiki/07-知识库技能表情包与脚本.md:11` | 不适用 | 找到。对不上 #63 |
| 19 N1 未选 | 旧版一个统一的目录，默认知识库另走一套 git 更新 | `docs/wiki/07-知识库技能表情包与脚本.md:11`、`:38-46`；`crates/miyu-engine/src/default_kb.rs:12-20` | 不适用 | 找到 |
| 19 N6 未选 | 旧版出厂默认装上 Shorin Wiki | `docs/wiki/07-知识库技能表情包与脚本.md:11`（默认知识库来自仓库 `kb/` 加 Shorin Wiki）；`crates/miyu-engine/src/default_kb.rs:12` | 不适用 | 找到 |
| 20 §一 V2；V2 未选 A | 旧版在用 GPT-SoVITS（能克隆音色、首音快、要显卡） | 悬空提交 ba3e27a（08-15，不在任何分支上）的说明与 `ba3e27a:docs/voice.md:197-205`；主线 `docs/plan/2026-09-05-voice-v2.md:13`；〔B〕`config/voice.rs:153` | 08-15 voice 分支；RTX 4060；首音约 100–200ms，热延迟约 0.95 秒/句，显存 1.4GB；样本原文没写 | 找到（出处只在悬空提交里，公开仓库多半看不到）。对不上 #64 |
| 20 §三 | 旧版也这样拆：主程序里没有一行语音代码，体积不变 | 提交 f7bb875c（09-05，「主程序 miyu 不链接 sherpa」）；`docs/voice.md:14-26`、`:360`；`Cargo.toml:20-25`；`crates/miyu-hosts/src/web/voice_bridge.rs:1-17` | 09-05 release；miyu 58.1MB「与无语音版持平」，miyu-voice 35.9MB | 部分：体积那半句对。对不上 #65 |
| 20 §四 第 1 条 | 旧版实测：待机 CPU 0.4%，内存 75 到 80MB | `docs/voice.md:327-347`、`:358`；`docs/plan/2026-09-05-voice-v2.md:106-111`；原始数据 `testkit/voice/measure-v2-full.json`、`testkit/voice/measure-v2-2core.json`；量尺 `testkit/voice/measure.py` | 09-05 release、16 核，CPU 型号原文没写；单跑 `miyu-voice test --timings`（不连 daemon）；隔离 MIYU_HOME，PipeWire null sink 加 pw-link 注入；安静段 40 秒，每 0.5 秒读一次 /proc（n=80），CPU% 以单核为 100%；原文注明机器同时在并行编译；限 2 核用 taskset | 找到。对不上 #77 |
| 20 §四 第 3 条 | 旧版实测：环境音和视频的声音会被识别成碎片，当成指令 | `docs/plan/2026-09-05-voice-v2.md:16`；〔B〕`config/voice.rs:35-36` | 原文没写谁测、何时、多少样本 | 找到。对不上 #76 |
| 20 §四 第 5 条；V6 | 追问窗口：播完以后 30 秒内接着说不用再叫；每回答一次重新计时；思考和说话时不计时 | 提交 37555677（09-06）；〔B〕`config/voice.rs:42-45、335-336`；`crates/miyu-engine/src/voice/pipeline.rs:285-288、313-317`；`docs/voice.md:207-211` | 是规矩，不是测量 | 找到。默认值变过两次：voice 分支 15 秒 → 300 秒 → 30 秒 |
| 20 §四 第 6 条；V6 | 开口就打断：约 0.3 秒人声她立刻停；旧版原来要等你整句说完才打断 | 0.3 秒：`crates/miyu-engine/src/voice/pipeline.rs:37-38、341-347`；`crates/miyu-engine/src/voice/mod.rs:31-33`；`docs/voice.md:92`；「原来要等整句说完」未找到出处 | 0.3 秒的来由原文没写 | 部分。对不上 #66。「原来等整句」只在本机 auto-memory `miyu-voice-feature.md:31` |
| 20 §四「唤醒词跟着人格」 | Miyu 的唤醒词是「未有未有」「密友密友」「miyumiyu」「みゆみゆ」 | 〔B〕`config/voice.rs:273-278`；`docs/voice.md:238`；提交 0fd1b11c（09-06） | 配置默认值 | 找到。旧版这是全局配置 `voice.wake_keywords` 的默认值，不跟人格；0fd1b11c 之前默认只有「未有未有」 |
| 20 §五 | 听写期间唤醒暂停，退出后恢复，旧版就是这样 | `docs/plan/2026-09-05-voice-v2.md:57、85`；`docs/voice.md:223、231`；`crates/miyu-hosts/src/web/voice_bridge.rs:863-874`；`crates/miyu-engine/src/voice/pipeline.rs:246-264` | 原文没写 | 找到 |
| 20 §六 | 旧版有一次模型在语音之后回了零宽空格，发出一条看上去空白的消息 | 提交 93b46023（09-06）；〔P〕`reply.rs:166-170`；`docs/voice.md:188-190` | 09-06 QQ 群里（「被人调侃『叛逆了』」）；次数原文没写 | 找到。对不上 #74 |
| 20 §七 模型文件 | 三样模型一共约 190MB；旧版放在每个数据根里，换数据根就重下一整份 | 提交 acc376e8（09-21）；`docs/releases/0.6.2/changelog.md:37`；`docs/voice.md:43-49`；`crates/miyu-engine/src/voice/worker.rs:572` | 09-21 流量统计里五个 miyu-voice 进程各收 188MB；三样分别 0.6MB、31MB、156MB | 找到。对不上 #67 |
| 20 §七 识别模型 | 加载后内存多约 300MB；旧版实测识别 3 到 5 秒的语音，热的时候约 150 毫秒，两核和十六核差不多 | `docs/voice.md:86-87`、`:349-360`；`docs/plan/2026-09-05-voice-v2.md:112-114`；同上两份 JSON | 同 §四 第 1 条；每轮 14 次热识别，音频 0.51–4.99 秒；最大值 7106ms、2288ms 出现在并行编译时（voice.md:355）；识别线程当时默认 2（`f7bb875c:src/config/mod.rs:167-169`） | 找到。对不上 #68。内存按 JSON 复算：常驻加 205–228MB，高水位加 321–336MB |
| 20 §八 表 Linux 行 | 旧版因为测试工具挪过一次播放流，之后所有声音都进了一个空设备 | 提交 0fd1b11c（09-06，「09-06 事故修复」一段）；`testkit/voice/sync_timing.py:113-119、142-148`；`docs/voice.md:285-295`、`:322-325` | 09-06 用户本机 | 找到。对不上 #75 |
| 20 §八 | 同一台机器开两个 Miyu，旧版按名字接线，接到了另一个 Miyu 的麦克风上 | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-voice-feature.md:23`（08-15）；公开的只有事后的做法：`testkit/voice/measure.py:72-73`、`docs/voice.md:283-284`。对不上 #69 |
| 20 V2 未选 B | 旧版试过本机 CPU 合成，因为音质太差删掉了（设计列的是 espeak-ng、Windows 系统朗读） | 悬空提交 ba3e27a 的说明与 `ba3e27a:src/voice/tts.rs:1-4`；主线 `docs/plan/2026-09-05-voice-v2.md:187-188`（d08ee92f，09-05） | 08-15 与 09-05 两次，都是用户耳听；样本原文没写 | 找到。对不上 #70 |
| 20 V3 未选 | 旧版的语音自己有一套会话和回合 | `docs/voice.md:105-107`；`crates/miyu-hosts/src/web/voice_bridge.rs:470-495`、`:521-543` | 原文没写 | 部分。对不上 #71 |
| 20 后续再定 第 1 条 | 旧版的唤醒词按拼音编，日语母语的发音一次都没命中 | `docs/voice.md:51-66`；提交 0fd1b11c；`crates/miyu-engine/src/voice/keywords.rs:13、259` | 09-05；MiniMax 合成的样本：「密友密友」5 种中文音色 5/5，日语音色 6 条、英语音色 3 条全不中；试过 100 种声调组合、阈值 0.05、加分 3.0；识别模型是普通话 wenetspeech 3.3M | 找到。对不上 #72 |
| 20 后续再定 第 2 条 | 提示音旧版有三套候选，要耳朵听过再定 | `docs/plan/2026-09-05-voice-v2.md:137-143`；`testkit/voice/sounds/synth.py:1-3`；提交 f7bb875c；`docs/voice.md:215-217` | 09-05 用户耳选 | 找到。对不上 #73 |
| 21 §二 首句 | 旧版网页一共 4.2 万行：`app.js` 13,887 行，样式 11,464 行，手抄的设置字段表 3,501 行 | 现场核实 | 2026-09-25，HEAD 8c18c5d5，命令 `find web -type f \| sort \| xargs wc -l`：`web/app.js` 13887、`web/styles.css` 11464、`web/settings-schema.js` 3501；自有 27 个 .js 合计 30,978，加 styles.css 是 42,442，再加 index.html（801）是 43,243；web/ 全部文件 47,851 | 找到。对不上 #78 |
| 21 §二 表后要点；X4 未选 | 手抄的字段表是从终端设置页的中文字符串里抽出来的第二份，运行时改了字段位置它没跟上 | 同 14 §二 那一行 | 同 14 | 找到（引 14）。对不上 #39 |
| 21 §三「行数上限」 | 旧版的 `app.js` 就是这样一点点长大的 | 现场核实：`git rev-list -1 --before=<日期> main` 取提交，再 `git show <sha>:web/app.js \| wc -l` | 07-25 创建时 4,370 → 08-01 前 6,031 → 08-10 前 8,703 → 08-20 前 10,355 → 09-01 前 10,779 → 09-10 前 10,129 → 09-15 前 13,060 → 09-20 前 13,120 → HEAD 13,887 | 找到。09-10 前后回落过一次 |
| 21 §三 X2 | 打包不需要 Node，旧版的小部件大多能直接搬过来 | 仓库根和 `web/` 下都没有 package.json；`web/README.md:3`；`web/lightbox.js:13`、`web/diff.js:16`、`web/linkcards.js:22`、`web/selectionmenu.js:18` | 本地 HEAD | 找到。它们是挂在 window 上的立即执行函数，不是 ES 模块，搬过来要改导出方式 |
| 21 §二 表「带过来」第 4 行 | 划词解释与翻译、链接卡片、图片灯箱、代码高亮、diff | `web/selectionmenu.js:3`（7e74837e）、`web/linkcards.js:4`、`web/lightbox.js:4`、`web/highlight.js:4`、`web/diff.js:4` | 本地 HEAD | 找到 |
| 21 §二 表「扔掉」第 2、4 行 | 网页自己算时间线；每个仪表盘各拼一套数据 | `web/app.js:831`、`:972-982`、`:1013-1060`；`crates/miyu-hosts/src/web/dashboards/mod.rs:1-5` | 本地 HEAD | 找到 |
| 21 §四；X3；X1 未选 B | 时间线规格：1px 细线在图标处断开、开口就断；默认收起；用时紧跟；失败整行红、详情一圈细边框；「已思考」加尾巴；无气泡、浅底、不显示时间；行高 30px；次要色六四开；淡入约半秒、展开约 0.4 秒、位移不超过 4px | 提交 024296e8、c4b9496a、97af7019、4b92528d（都在 09-10）；`web/styles.css:10613-10654`、`:10694`、`:10720`、`:10750`、`:10849`、`:10934-10977`、`:11014` | 024296e8：webui-timeline 十项判定（沙箱 daemon、工具剧本桩、Playwright）；模型原文没写 | 找到。对不上 #79、#80 |
| 21 §四「否决过的，别再提」；§四首句 | 九条否决清单（项目主人：「完全是灾难」）；「一条条跟项目主人对过」 | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-webui-process-timeline-2026-09-10.md:35`、`:43`。公开旁证两条：`docs/releases/0.6.0/changelog.md:5`（旧画法是「十块彩色方块」）、`web/styles.css:10616`（「没有底板、边框、标签色」） |
| 21 §五「软件包可以带自己的页面」 | 旧版的记账、表情包仪表盘，就是软件包在清单里声明、带进来的 | `crates/miyu-hosts/src/web/dashboards/mod.rs:1-5`；`crates/miyu-hosts/src/web/assets.rs:464-498`；`web/index.html:33-42`；`docs/plan/2026-09-04-dashboard-design.md:254`；`docs/plan/2026-09-03-plugin-dashboard-and-io.md:13` | 本地 HEAD；设计文档 09-03、09-04 | 找到。对不上 #81 |
| 21 §五「手机」 | 旧版专门为手机调过样式 | 提交 8ada5a04（09-04）、e199fe33（09-09）、d1dd2b7c（09-11）、fc624f4a（09-24）；`web/styles.css` 里 39 处 `@media`；`testkit/settings-ui/mobile-shoot.js` 等三个脚本 | 8ada5a04：390px 屏宽走查 | 找到 |
| 21 §二 表「主题」；§六 | MD3 语义色；出厂晨光、夜阑两套，夜阑默认；旧版靠 matugen 生成一份 CSS 覆盖 | `web/README.md:19-35`；`web/styles.css:31-36`、`:156-160`；`web/app.js:13833`；`resources/matugen/README.md:1-13`；`resources/matugen/miyu-theme.css:1-9`；`crates/miyu-hosts/src/web/server.rs:412、794-795` | 本地 HEAD | 找到。`web/README.md:33-35` 里的路径和「路由待做」已过时 |
| 21 §七「本机打开」；X6 未选 | 旧版在本机不设密码谁都能进；同机任何程序包括沙盒里的命令都能打开网页冒充你 | 09-11 以前：`4c03aed2:src/web/server.rs:141`、`:182-186`；`web/README.md:9-17`；`crates/miyu-hosts/src/runtime/state.rs:331-335`；09-11 起必须登录：提交 c78022ae、3529d062，`crates/miyu-hosts/src/web/server.rs:136-141`；「沙盒里的命令也能冒充你」未找到出处 | git 历史与代码 | 部分。对不上 #82 |
| 21 §七「产物」；§八 第 4 行 | 浏览器的私有网络访问限制比内容安全策略先生效，拦下产物里的请求，报错却指向内容安全策略 | 提交 f5cc7613（09-11）；`docs/plan/2026-09-11-artifact-delivery.md` §3.1（:266-291，报错原文 :268-272）、§2.2b（:156-176） | Chromium 151 会拦，Firefox 153 不实现这项限制；本机 127.0.0.1；webui-artifact 16 项，两个浏览器各 16/16 | 找到。对不上 #83 |
| 21 §八 第 1 行 | Safari 里给滚动容器设了 `container-type`，滚动位置被清零，页面一直抖 | `docs/plan/2026-09-10-four-items.md` §2（:24-42，根因在 :26）；提交 3dea4fde（09-10）；`docs/releases/0.6.0/changelog.md:68` | WebKitGTK 2.52（与 Safari 同引擎）加 Chromium，webui-jitter；修前 WebKit 最大跳动 3676px，修后 1px；没有真机 Safari 记录 | 找到。对不上 #84 |
| 21 §八 第 2 行 | 两个标签页同时删掉最后一个会话，各自兜底新建一个，冒出两个新会话 | 提交 d9e087ef（09-10）；`crates/miyu-hosts/src/web/sessions/http.rs:292-296`；`docs/releases/0.6.0/changelog.md:78` | Playwright 双页复现；样本原文没写 | 找到。对不上 #85 |
| 21 §八 第 3 行 | 换会话时用量的基线只升不降，没按会话清 | 提交 767635eb（09-23） | webui-delete-footer 修前 5/7，修后 7/7；原文「用户验收通过」 | 找到 |
| 21 §二「扔掉」第 5 行；X5 未选 | 页面编进二进制，也就是旧版的做法，改一行页面就要重新编译 | `crates/miyu-hosts/src/web/assets.rs:1-6`、`:464-498`；`AGENTS.md:82`；`web/README.md:3`；`docs/plan/low-footprint.md:102`（「资源外置判不做（用户 08-21 拍板）」） | 本地 HEAD | 找到 |
| 22 §三；O2 | 旧版考虑过让一个环境变量当默认会话（她调用自己时会递归进正在跑的会话），没有采纳 | `docs/cli-backend.md:151-152`；已删的计划 `b3d4533c^:docs/plan/2026-09-07-cli-overhaul.md:80`（原提议）、`:219-220`（「风险大于便利。不做。」）；提交 3a2d7a03（09-07） | 09-07 | 找到。`MIYU_SESSION` 在旧版里仍给工具桥认会话用（`src/cli/tool_cmds.rs:43`） |
| 22 §四；O4 | 旧版做过「替换系统提示词」「追加系统提示词」两个选项 | `src/cli/args.rs:186-192`；`docs/cli-backend.md:26-27`；`b3d4533c^:docs/plan/2026-09-07-cli-overhaul.md:263-267`（用户 09-07 裁决「都做」）；提交 3a2d7a03 | 09-07 | 找到 |
| 22 §七 O6；O6 已定 | 以旧版现成的 fish 配置为底：回车判断是命令还是一句话、多行交给她；Ctrl+J 换行；Ctrl+V 粘贴显示成 `[Image 1: 截图.png]` 这样的占位符；找不到命令兜底交给她 | 〔B〕`shell/fish.rs:246-293`（回车）、`:112-120`（Ctrl+J）、`:93-110`（Ctrl+V）、`:295-320`（兜底）；`src/cli/shell_bridge.rs:74-131`、`:168-176`；`docs/wiki/03-使用方式.md:111-117`；提交 c54f9f3c（07-05）、442d8475（07-11） | 不是测量 | 找到。对不上 #86 |
| 22 §七「装」 | 更新时顺着符号链接写，旧版出过把链接换成普通文件的事 | 同 14 G5 第 3 条那一行 | 同 14 | 找到（引 14）。对不上 #40 |
| 22 §七「她在哪个会话里」 | 旧版叫「终端集成会话」；旧版的默认模式，就是现在这一项里的预设 | 提交 c1aa722d（08-15）、32fa1e56（09-18，`terminal_session_mode` 取 normal 或 dev，默认 normal）；〔B〕`config/defaults.rs:495-497`；`docs/releases/0.6.1/changelog.md:251` | 不是测量 | 找到。旧版终端那条路落在 daemon 的 current_session 指针指着的会话上，不总是固定的一条（32fa1e56） |
| 22 §七「输出」 | 旧版一开始每帧先擦再写，屏幕一闪一闪 | 提交 1dc4a841（09-17）；`crates/miyu-hosts/src/render/wait_spinner.rs:106-111`、`:160-162`（「用户实测『流式输出的时候一闪一闪的』」）；`docs/releases/0.6.1/changelog.md:134` | 用户实测；30 行 PTY 探针，擦行次数 7202→819（长段落）、4456→481（多行） | 找到。旧版的修法有两样：同步输出块，以及只重写变了的行；设计只写了前一样 |
| 23 §二 表（范围外） | 旧版的全屏演示放五条长回复不到 4MB | 同 13 §九 那一行 | 同 13 | 部分（引 13）。对不上 #30 |
| 23 §三；未选 B（范围外） | 旧版测过 mimalloc，判了不做 | `docs/plan/low-footprint.md:69`、`:84` | 默认参数时 daemon 落定 104MB（glibc 60MB），高水位 105 vs 63.5；调参后落定 58.5，回合后 65.1 vs 50.3；数据在 `testkit/low-footprint/results/`（被 .gitignore 排除，不在仓库里） | 找到 |
| 25 W1 说明（范围外） | 旧版实测过「结构壳」：留参数名和类型、去掉参数说明，体积省一半，约束解码的模型也能用 | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-tool-loading-per-model.md:17`（结构壳 10.2KB 对全量 21.4KB，「3/3 验证可用」） |
| 25 W1 说明（范围外） | 旧版的账：40 件工具全量 21.4KB，参数说明占 16.8KB，工具说明只有 4.6KB | 未找到出处 | — | 未找到。同上，只在 `miyu-tool-loading-per-model.md:17` |
| 25 约束解码一节（范围外） | 存根的宽松外壳只能发出 `{}`，没声明的工具名也调不出来；旧版对照 8 次全部如此，契约写进对话也救不回 | 提交 ac1bb8de（09-01，「裸 API 8/8 复现」） | 同 08 C5 | 部分：「8 次全部如此」「契约救不回」找到；「没声明的工具名也调不出来」只在本机 auto-memory `miyu-tool-loading-per-model.md:10` |
| 25 未选 A（范围外） | 旧版对六十件左右的目录全部全量声明 | 提交 e6830141（09-01，「默认工具加载模式改 full」，说明里「对 Miyu ~60 工具量级的目录，stub 的省 token 优势本就薄」） | glm 62 件工具：全量 11/12，存根 8/9 | 找到。旧版是默认全量，存根仍可按模型选 |
| 25 未选（范围外） | 教模型用文本格式发工具调用，旧版试过可行 | 未找到出处 | — | 未找到。只在本机 auto-memory `miyu-tool-loading-per-model.md:10`（「文本协议（教 `<miyu:invoke>` 格式）3/3 可行」） |

## 统计

- 表里一共 191 行：00 到 22 有 184 行，范围外的 23、25 有 7 行。
- 00 到 22 的 184 行里，5 行是 Claude Code 的外部参考，其余 179 行是旧版证据。179 行里有 6 行是转引另一行（00 的 25 个字段、06 U8、16 Y8、19 N4、21 字段表、22 符号链接），其余 173 行各有各的出处。
- **179 行旧版证据：找到出处 169 行，其中 145 行出处完整、24 行只找到一部分；未找到 10 行。**
  - 未找到的 10 行是 9 件事（16 Y8 转引 18 的 issue #49）：00 SOLID 评分；07 测试误用真实数据目录；15 项目主人那句价格原话；17 错的记忆修好 bug 后还在；18 有意把平台工具从终端拿掉、issue #49、读了几十分钟 NapCat 源码其实连的是 SnowLuma；20 按名字接线接到另一个 Miyu 的麦克风；21 时间线否决清单与「完全是灾难」。
  - 这 9 件里，6 件只在本机 auto-memory 有记载（不公开）；issue #49、SOLID 评分、「有意拿掉」3 件在两个仓库和 auto-memory 里都没有原始记录（SOLID 评分据新仓库的审阅回应，出自审阅者的截图）。
  - 24 行「部分」里没找到的那一半，备注里都写了。只在本机 auto-memory 有的例如：01 跨端切换掉缓存、08 示范对话单用时的 A/B 结果、12「把自己也结束了」、12 首版资产缺字体、13「110×32、3.4MB」、20「原来等整句说完才打断」。哪里都没有的例如：13「换了模型」是铃铛行、「一句开场白」、「数字也慢一拍」；18「撤回、删除两处各做一遍」「靠提示词叫她别回」；21「沙盒里的命令也能冒充你」。
- 5 行外部参考：找到 4 行，部分 1 行（「砍最老的 20%」）。
- 范围外 7 行：找到 2 行，部分 2 行（其中 1 行转引 13），未找到 3 行（25 的「结构壳」、40 件工具的账、文本格式调用，都只在本机 auto-memory）。
- 对不上的地方一共 86 处，见下一节；表里每一处都标了编号。
- 可复核性：本地 main 比 origin/main 多 76 个提交。表里引用的下列出处不在 origin/main 上，公开仓库读者看不到：
  - 本地 main 上、未推送：案卷 `docs/fixed/2026-09-24-缓存回放与活体对不上.md`（fb6fb01f，09-24）；提交 a2bb3b4a、afafe349、950aa898、69eb4d1f、6645f956、b3048739、b92bcc7a、d0830016、fc624f4a。表里其余提交都在 origin/main 上（逐个核对过）。
  - 不在任何分支上：af95161e、770bc476（只在本地标签 backup/render-golden-before-rebase 上，内容已由 c9313cb0 移植）；ba3e27a（悬空提交，voice 分支删掉后留下的）。
  - 只在分支 worktree-cache-live-2026-09-25 上：62418a08（WIP）。
  - 文件：表里引到的约 200 个路径逐个用 `git cat-file -e origin/main:<路径>` 核对过。只在本地 HEAD、不在 origin/main 上的有：上面那份案卷、`crates/miyu-engine/src/agent/tests/replay_prefix.rs`、〔RC〕`restraint.rs`、〔P〕`plugins/renderer/columns.rs`、`src/cli/repl/job_stop.rs`、`testkit/tui/direct_overlay_keys.py`、`testkit/tui/other_session_jobs.py`、`testkit/cache-forensics/replay_live.py`。`testkit/persona-ab/results/` 与 `testkit/low-footprint/results/` 被 .gitignore 排除，本地也没进仓库。其余都在 origin/main 上；代码行号按本地 HEAD，公开仓库的行号可能错开。
- 没收进表的：设计文档里有一些照搬旧版做法、但没写「旧版」的规格（例如 18 §五的限流与睡眠、§八的清理与表情包、§九的平台工具表），它们的出处多在 wiki 13 与管线文档；外部项目的事实（Codex、dsh「约 17 万行 React」、pi、opencode、jcode）；项目主人 2026-09-25 的口述与本轮决定。

## 转述与原文对不上的地方

1. **01 D2 未选**：设计把「单次命令退出时会杀掉后台任务」叫作旧版教训。旧版这是有意保留的语义：提交 717bace6（08-15）让前端退出不再取消回合，但写明「/jobs 的显式停止手势与一次性会话的阅后即焚语义保留」；`AGENTS.md:70` 把它当测试注意事项（测唤醒用 shellhook 形态）。不是事故。
2. **04 P4 未选**：「一条规则写三遍」没有对应记录。命令清单 08-18 以前是终端、网页各一份（`crates/miyu-core/src/slash_commands.rs:3-5`：「两份迟早分叉」），之后共用一张表（cb28acd5）；QQ 一直另有自己的解析器（〔P〕`commands.rs:98`）。
3. **05 §九、08 C4**：条件被省略。「工具被移除读成掉线」发生在 claude-code 中转线上：一条 Miyu 会话复用一条 `--resume` 的 claude 会话，Claude Code 的 `deferred_tools_delta` 报「46 个 mcp__miyu__ 工具被移除」。原文说此后「不再碰任何 Miyu 工具，只用 claude 原生 WebSearch」，设计写成「不再调用任何工具」（db101d05）。
4. **07 §六**：归因简化。278ae076 写的根因是 08-18 引入的「open 时 incremental_vacuum 对 wal/主文件配对破坏零容忍」；fs::copy 备份丢掉本进程的常驻 WAL 锁，是造成配对破坏的一条途径。设计只写了丢锁。
5. **07 §八、S2 未选 A**：`AGENTS.md:54` 标「全库最脆弱处」的是改 Turn 字段时要同步的固定列序 SELECT 与 `map_turn_row`；迁移那条（:54 前半）是规矩，没说它最脆弱。
6. **08 §一、C1 未选**：「最常见的一类缓存事故」没有数据支持。案卷量出的未命中里，这一类（历史前缀被改写）是 82 万 token、12%，五类里最少；最多的是换进程或新会话的首请求（283 万，41%）。同类问题确实反复出现过（09-23 剪枝、09-24 回放、09-24 追记一）。
7. **08 §三 system**：「旧版实测它对保持人格很有效」省了条件。仓库里的数据只有 71e48dc7 的「闲聊中位 63→48 字」，和 189a3909 的一句「A/B 实证干净体制预设对话已足够」（原始结果被 .gitignore 排除）。本机 auto-memory 记的 08-14 A/B 里，在 31k token 的真实群聊历史下，单用示范对话 4/12，「单用基本无效」；和尾部提醒合用才是 8/12。
8. **08 C9**：省了条件和后半句结论。76.3% 对 87.6% 只测了一个模型（opencodego 上的 mimo-v2.6-flash），每组 16 次请求。原文接着写「命中率不是账单」：剪枝组的 prompt 只有对照组的 59%，缓存读按 1/10 价折算反而省 12.3%，只有缓存读完全免费时才更贵（`docs/plan/2026-09-23-cache-forensics.md:145-157`）。旧版最后的修法（08286954）是把剪枝挪到压缩那一刻，不是挪到执行时；设计选的「执行时定预览」对应原文说的「真正的最优解」（:159-162）。
9. **08 §五「浮动尾巴只损失它自己」**：省了后续。99.6% 是 08-14 量的；两天后提交 189a3909（08-16）让浮动尾巴退役，理由是「它每轮在自身原位掰断前缀」，提醒改成每 3 轮一次、以化石身份进历史，默认还关掉了。旧版量过浮动尾巴，最后没用它。
10. **08 C2、C2 未选，17 §四**：条件被省略，结论被推广。80%→100% 是 deepseek-v4-flash 一个模型、QQ 群聊、「跨轮持续指令」这一类场景（原文说简单场景有 97% 的天花板，看不出差别），N=32 没说是每组还是合计。实验挪的是「群聊记录块」和当前消息的相对位置，没有测时间、记忆召回这类注入放前放后。设计据此让一切事实型注入排在触发消息之前，17 也据此把召回挪到消息前。而旧版把这些注入放在消息后，是为缓存特意安排的（6599404b；`docs/wiki/08-记忆系统.md:29`）。
11. **08 C7**：结论被放大。原文只测了单独改写 `<qq-identity-policy>` 的尾句（p=0.83，「完全无效」）。设计写成「行为禁令式的措辞没有作用」。
12. **08 §六 保温**：「在别的供应商上零收益」是从一家推广的。原文只对比了 opencodego 上的 mimo（97.2% vs 97.3%），另说按次计费网关「白烧请求额度」。样本量原文没写。
13. **08 §六 钉住端点**：「旧版是一个全局开关」与代码不符。`llm.cache_sticky` 只是 v7 计划里的开关（`docs/cache-and-prompt-plan.md:119`，默认关），列在「规模门控（后续批次）」（:216）。HEAD 代码里没有它（`grep -rn cache_sticky crates src` 无结果），端点调度一直是每池轮换（`crates/miyu-core/src/llm/openai_compatible/endpoints.rs:60-111`）。
14. **09 §六、Z7（外部参考）**：「先砍掉最老的 20%，最多 3 次」在旧仓库里找不到。`docs/compact-plan.md:36` 写的是「reactive 重试按溢出量播种」；「每次砍头 20%/按 gap」只在本机 auto-memory `claude-code-compact-internals.md:14`。
15. **10 §二**：归因只取了一半。`docs/compact-plan.md:212` 的根因有两条：回灌候选的工具名单停在旧名（08-21 改名以后，edit 的路径藏在补丁头里，认不出来），以及只读文件全走 `run_command cat`。那个会话 6 次 edit、0 次 read；修法是认补丁头，不是改掉 cat。
16. **10 B10、19 §四、N4**：翻车的不是 `read`。旧版现行规则保留了 `read` 的 `kb:` 前缀（`AGENTS.md:47`；`docs/wiki/06-内置工具与插件.md:71`），把写入拆成独立的 `kb` 工具（0fc813db）。本机 auto-memory `miyu-token-diet-2026-08-21.md:37` 记的是：统一 edit 的 kb: 前缀实测翻车，模型想不起前缀，被 remember_fact 的名字劫走。
17. **10 §九**：时间和理由都不对。「按模型选全量或存根」和「删掉 hybrid」在同一个提交里（ac1bb8de，09-01），不是先后两步；删 hybrid 的理由是它按已加载的集合增长工具数组、和前缀缓存相克，不是约束解码。旧版的 hybrid 指「用到才加进 tools 数组」，和重制版「按件配全量或存根」的混合预设是两回事。
18. **12 R5**：仓库里记的是测具脚本的事：`pgrep -f` 匹配到自己的等待循环（`docs/plan/2026-09-23-cache-forensics.md:229`），`ps \| grep` 后无差别 kill 杀掉了另一个会话跑 macOS CI 的包装进程（:242-246；290c03e9）。「把自己也结束了」只在本机 auto-memory `miyu-voice-feature.md:33`（pkill -f 匹配到自己的 bash 包装）。都不是 Miyu 自己管子进程时出的事。
19. **12 R4**：原文是 08-16 代码审查的黑盒探测，打的是一条不存在的子命令 `alarm --help`，没设 MIYU_HOME，被当成对话发给了生产 daemon（`docs/code-review-2026-08-16.md:236`）。不是人手打错。
20. **13 §三 规则 1**：顺序不同。旧版终端是「名字 · 用时 · 对象」，代码注释专门解释了为什么这样排（〔T〕`mod.rs:709-718`；`docs/releases/0.6.0/changelog.md:14`）。「对象在前、用时在后」是旧版网页的顺序（4b92528d）。
21. **13 §三 规则 2**：旧版全屏里点开的详情是空行加缩进 4 格，不带竖线（〔T〕`glyphs.rs:101-104`、〔T〕`live.rs:198-211`）。`│ ` 前缀只用在 shellhook 和单次命令的静态时间线，以及命令的尾巴行。
22. **13 §三 规则 4**：「永远英文」有例外：没有计时也没有动作时，旧版回落到跟界面语言走的「已完成」（〔T〕`live.rs:668`）；网页只想过时写 `1 thought`，不写 `Thought for`（`web/app.js:1013-1016`）。
23. **13 §三 规则 5**：旧版终端结束那一行是「已思考 · N 词元 · Xs」，不带摘要（〔T〕`mod.rs:877-885`），用户 09-17 明确拍板「都不带窥视」（`docs/plan/2026-09-17-render-unification.md:43`）。带尾巴摘要的是旧版网页（97af7019）。
24. **13 §三 规则 7**：旧版普通模式的 `┃` 是蓝色粗体，洋红只在开发模式（`src/cli/footer.rs:90-95`；`docs/plan/2026-09-10-tui-rewrite.md:52`）。
25. **13 §三 规则 8**：旧版换模型以后是右上角通知条「会话模型已更新，下一轮生效」，停 2.2 秒（`src/cli/repl/remote/slash_config.rs:193-203`），不是铃铛行；有摘要的压缩收成可以展开的 `› 上下文已压缩` 块（〔T〕`live.rs:7-26`）。
26. **13 §三 规则 9、§十 第 8 行**：「只用三个符号」只对工具图标成立。没有 Nerd Font 时旧版还用 ✗、✳、↻、≡（〔T〕`mod.rs:101-147`、`crates/miyu-hosts/src/render/style.rs:63-68`）。旧版也不探测字体，只认环境变量 `MIYU_TUI_ASCII=1`。
27. **13 §三 表**：归因不同。旧文档说的「收缩有三份实现」全在终端里：主线、前台子代理面板、后台子代理面板（render-unification §2.1、§1.2），09-17 已合并。网页另有一份，而且网页里子代理的过程根本不收起（`web/app.js:978-981`）。
28. **13 §七、H7**：「旧版是这样」只对「列在底栏下方、逐条列」成立。旧版没有 3 条上限，也没有「等你确认」行；用时右对齐，新版写的是紧跟；种类用文字加任务编号，不是图标（`src/cli/repl/jobs.rs:17-19`、`:80-89`）。
29. **13 §七「停止」**：原文是「任务要等这一轮说完才停」（`src/cli/repl/job_stop.rs:3-6`），不是停不掉。它和直连模式的同类问题都在 09-24 修掉了（69eb4d1f、950aa898），HEAD 上已经不是这样。
30. **13 §九（23 §二同）**：公开记录是 110×30 下 RSS 4.0MB（`testkit/tui-demo/README.md:34-36`），不是「不到 4MB」；施工单另记 3.8MB，没写尺寸。「110×32、五条长回复、3.4MB」只在本机 auto-memory。那是一个独立的演示程序，回复是伪造的，不接 daemon；产品的全屏终端在 200×60 下整进程约 34MB（`docs/plan/2026-09-11-tui-implementation.md` §7）。
31. **13 §十 第 2 行**：产品里是最多 4 条，加上下两条框线共 6 行，不滚动（`src/cli/repl/commands.rs:123`、`src/cli/repl/tail/screen/toast.rs:182`）。滚动的 4 行窗口只在演示程序里有。
32. **13 §十 第 3 行**：产品从 08-23 起图片占位符只有 `[Image 1]`，不带文件名（46889bb1；`src/cli/repl/editor.rs:480`）。带文件名的格式只剩 shell 集成和演示程序里有。
33. **13 §十 第 4 行**：「绿框、输入框上方、2.5 秒」只存在于演示程序（`testkit/tui-demo/src/main.rs:450`、`:1331`）。产品里没找到复制提示，拖选后只写 OSC 52；产品的通知条在右上角、停 2.2 秒、框是淡紫色（`src/cli/repl/tail/screen/toast.rs:29-33`、`:98-103`、`:219-224`）。
34. **13 §十 第 5 行**：「一句开场白」没找到。大厅里只有副标题 `A G E N T`、模式行和提示行。
35. **13 §十 第 6 行、§六**：省了一个条件：旧版要在 2 秒内连按两次 Esc 才打断回合（`src/cli/repl/editor.rs:274-287`）。
36. **13 §十 第 7 行**：旧版通知的是「回复完成」和「等你回答」（提问），没找到「等你确认」。
37. **13 H1**：日期不对。09-11 是在分支上开工；09-13 合进 main（e9bf333f），但要设 `MIYU_TUI=1` 才开，inline 仍是默认；09-14 起默认全屏（9142225c，随 v0.6.0）。
38. **13 H9**：「数字也慢一拍」在任务表上找不到出处。最接近的是 footer 上的会话累计词元被轮询套回旧数（`src/cli/repl/jobs.rs:187-192`，69eb4d1f 已修），说的是会话累计，不是任务表。
39. **14 §二、G1 未选 A，21 §二、X4 未选**：设置字段表的来历说得不全。字段表只有中文标签抄自终端设置页，默认值、范围、枚举抄自 Rust 配置（`web/settings-schema.js:3-4`）。a70c9ebd 里绑旧字段的是终端和网页两个设置页，21 §二那句只说了网页没跟上。embedding 在 08-10 提成全局，到 09-23 才修。
40. **14 G5 第 3 条、22 §七**：99c5194e 是在加 hook 自动更新的同一次改动里，用一条红测抓到「rename 会把符号链接换成普通文件」，同一个提交修掉（「符号链接那条在修之前是红的」）。自动刷新 hook 这条路是那次才加的，没有记录说这件事发生在用户身上。另外旧版 HEAD 里 rc 文件那条路（〔B〕`shell/mod.rs:89-110`）现在还是直接 rename，不跟随链接。
41. **15 §五、M4 未选**：理由的出处不一致。设计引项目主人 09-25 的说法「为了把各家的免费 API 轮着用」；旧版 v7 的决策记录写的是「默认轮转是按次计费供应商的刻意设计，用户已确认」（`docs/cache-and-prompt-plan.md:34`、`:170`）。两个理由可以并存。
42. **16 §一、Y6 未选**：两句各对一个时间点。旧版 dev 会话挂在保留人格 dev 名下（d76ee8bf，08-15）；它的提示词到 09-24 为止内置一句「You are a helpful software engineer assistant.」，09-24 起默认为空（afafe349）。「开发预设不带人格，是旧版开发模式的做法」只对最后两天成立；§一「旧版开发模式单独的那套提示词，现在是软件工程师这个人格」说的是更早的状态。
43. **16 Y2 未选**：只有内置的脚本和技能绑在出厂人格上（0a120b38，09-01）；用户放在全局层的脚本，所有人格共享（同一提交）。
44. **18 §一 表第 1 行**：三处都自己连模型，但重试只有两处：判官（〔RC〕`judge.rs:107`）和入群审批（〔P〕`onebot/group_join.rs:295`）。好感度那处只有超时（〔RC〕`affection/mod.rs:580-594`）。
45. **18 §一 表第 4 行**：14 种、11 个文件，只在「非测试文件、连注释也算」的口径下对得上。这 11 个文件里 10 个本来就在平台层里；平台层以外只有 engine 的 `input.rs:362` 一处注释，下层真正拼进提示词的是不带 qq 前缀的 `<context-images>`。按「真拼进提示词」数是 13 种、8 个文件。所以这条撑不起「QQ 细节漏到了下层」；能撑起来的是同一格里的头像地址和 `QqOutreachPort`。
46. **18 §一 表第 5 行、§六**：少说了旧版明写的取舍。〔RC〕`runtime.rs:71-72`：「只在内存里：半衰期是分钟级，daemon 重启丢了也几分钟内就追平」。水位本来就存了盘。
47. **18 §三「约 195 个配置项」**：数不出 195，设计也没说怎么数的。按结构体字段加（不算 legacy 字段）是 208：平台侧 54，插件 154（real_context 95、reply_processor 17、群管 16、撤回 6、表情 3、入群审批 6、消息历史 3、定时任务 6、识人映射 2）。按网页 `web/settings-schema.js` 数是 174。
48. **18 §三「光『要不要开口』就占了 95 个」**：95 是 real_context 插件的全部设置。其中真正管要不要开口的约 33 个，其余是好感度 28、情绪 13、审核 10、引用和 @ 5、贴表情 3 等。这个数还一直在变：5d83427a（09-04）写「真实上下文 94 项分八组」，同一天的计划写「真实上下文 120 项」。
49. **18 §三「存成不带类型的 JSON，每轮重新解析」**：只有插件设置是无类型 JSON，平台侧那 54 项是有类型的结构体。每次现解析的是 reply_processor、群管、消息历史、撤回、表情这些插件；real_context 的 95 项按实例配置缓存（〔RC〕`mod.rs:77-100`），配置不变就不重新解析。
50. **18 §四「进门之前没有按消息编号去重」**：分发那一侧确实没有，但少说了两件事：历史库写入用 `INSERT OR IGNORE` 加 UNIQUE 约束，同一条消息只入一次库；断线重连造成的重复回复靠连接代数挡住（〔P〕`onebot/connection.rs:21-24`）。
51. **18 §六「一两句几乎不抬」**：原文是「说一句几乎不压」（〔RC〕`restraint.rs:11`）。按那张表，连说两句门槛已经抬了 0.12，超过上限 0.35 的三分之一。
52. **18 §六「分数一旦摆在模型面前，她就会拿它立规矩」**：原文说的是「现成判据」，指 `mentioned_bot:false` 这个字段（302fb77f）和好感度的中文档位名（28ca1390），都不是分数。判官提示词里的加减分，原文给的结论是「重复计分」（b92bcc7a），没说她会拿它立规矩。
53. **18 §六 Q5 说明**：「不说明为什么叫她」：旧版对「概率抽中且判官放行」的回合注入了 `<qq-join-in>` 说明（b0db9859），其余四种触发没有。「不给她不说话的路」：旧版会吞掉空回复和只有括号旁白的回复（〔P〕`reply.rs:170、186`；〔P〕`turn_context.rs:455-470`），缺的是一个明确告诉她的通道。因果也不同：仓库提交写的直接原因是 `mentioned_bot:false` 字段（302fb77f）、同一人追发时抢占流程跳过判官直接回复（b3b84112）、照抄自己的历史（b0db9859）；「硬起回合加上没有不说话的通道」这个总归因只在本机 auto-memory `miyu-meta-refusal-2026-08-29.md:13-16`。
54. **18 Q5 未选「靠提示词叫她『不该回就别回』，旧版就是这样」**：在 HEAD 的提示词、平台层提示词和 git 历史里都没找到这样的话。旧版原文的立场相反：判官放行、回合起来以后，该不该回就不是人格模型的问题了（302fb77f、〔P〕`onebot/identity.rs:318-321`）；b0db9859 注入的是「照样接话」。
55. **18 §八 引用和 @**：阈值是 4 条、15 秒，但少说了两个条件：上一条是她自己发的，也会引用（e99dfcd8，〔P〕`reply.rs:66-68`）；@ 除了隔一段时间，还要这期间有别人说过话（〔P〕`reply.rs:70-76`）。
56. **18 §九、Q16 未选 A，16 §三 Y8、Y8 未选**：三处说法互相矛盾。18 §九说「终端里只有两件 QQ 工具」，和代码一致（`send_qq_message`、`qq_contacts`）；Q16 未选 A 说「终端里一概没有平台工具，也就是旧版的做法」；16 写的是「旧版终端里没有 QQ 的工具」。「有意从终端拿掉」没有原文，代码上是平台工具需要活的平台回合上下文，结构上进不了终端。issue #49 本身找不到。
57. **18 §十一 SnowLuma**：少了版本条件。这是 1.14.7 的毛病，上游 1.14.9 修了，本机 1.14.17 复验正常（cfe3b764）。
58. **18 §十一 NapCat 禁言**：归因不同。db4e27c1 写的根因有两段：一是旧版自己传了 `no_cache:false`，NapCat 只是照这个参数用缓存回答；二是旧版把「我被禁言了」按原定解禁时刻缓存，最长 31 天，解禁通知带来的 30 秒一过，就一直哑到原定时刻。设计把整件事算成了 NapCat 的毛病。
59. **18 §十一 视频**：归因不同。4d4f97df 把「看不了视频」归到旧版自己的三处断点：视频段没进文件列表、历史里没留编号、看图工具不认文件编号。「只有 get_file 认」只是修法里的一步。
60. **18 §十一 语音**：归类不对。原文（〔P〕`onebot/voice_inbound.rs:1-8`）是功能说明，不是哪个实现的毛病；「语音消息」是 0fd1b11c 随转写一起加的退化文案，识别不可用时显示它。「要 wav」说的是 NapCat 的 get_record 接口。
61. **18 §十一 加群 flag**：原文点名的是 SnowLuma（fece7ef0），设计写成「部分实现」。QQ 返回的原文是 `already deleted by system`，「系统已处理」是 wiki 的中文转述。
62. **18 Q14 未选**：进站检查不全在那个约 720 行的函数里。睡眠、谁能叫她、限流在 〔P〕`onebot/admission.rs:200-314`，由它在 `dispatch.rs:169` 调用；「她被禁言了」的判断在 decide_group_trigger 里（〔RC〕`inject.rs:123、277`）。更准确的说法是由它串起来、散在三处。
63. **19 §四**：旧提示词原文有条件：「当知识库和网络搜索的信息出现冲突时，判断网络信息的时效性，若时效性不足则以知识库的信息为准」（`e3129ffe^:src/prompts/miyu.md:51`）。这条 09-21 已从提示词里删掉（e3129ffe）。wiki 07:11 的「两者冲突时优先信知识库」也是简化后的说法。
64. **20 §一、V2 未选 A**：GPT-SoVITS 只在 08-15 的 voice 分支上用过（ba3e27a，没合入，分支已删）。v2（f7bb875c，09-05）起主线没有它，计划书还把「没配 GPT-SoVITS 参考音频就整体停用」列为 v1 的毛病（`docs/plan/2026-09-05-voice-v2.md:13`）。主线的播报供应商只有 MiniMax 和 MiMo。
65. **20 §三**：「主程序里没有一行语音代码」不对，原文是「主程序 miyu 不链接 sherpa」。主程序里实际有 voice_bridge.rs（1089 行）、voice_tts.rs（890 行，MiniMax 和 MiMo 的云端合成）、voice_api.rs、onebot/voice_inbound.rs、config_tui/voice.rs、cli/stt.rs、repl/dictation.rs。没有的是 sherpa-onnx、麦克风和识别模型。「体积不变」是对的。
66. **20 §四 第 6 条、V6**：主线旧版播报期间把麦克风帧整段丢掉，是半双工，理由是「没有回声消除，不这样它会听见自己」；0.3 秒起势只取消正在跑的回合，不停播放（`crates/miyu-engine/src/voice/mod.rs:80-82、186-188`；`crates/miyu-engine/src/voice/worker.rs:377-379`；`docs/voice.md:133-134`；d08ee92f）。所以旧版她开口说话以后，用声音打断不了。能在播放中打断的只有没合入的 voice 分支（ba3e27a）。
67. **20 §七 模型文件**：「放在每个数据根里」是 09-21 以前的情况。acc376e8（09-21）已改成机器级共享 `~/.cache/miyu/voice-models`，随 v0.6.2 发布（`docs/releases/0.6.2/changelog.md:37`）。旧版最后的做法就是 V4 现在定的做法。
68. **20 §七 识别模型**：152ms（16 核）和 168ms（限 2 核）是每轮全部 14 次热识别的中位数，音频 0.51 到 4.99 秒。只取 3 到 5 秒的那几次：16 核 167ms（n=7），2 核 258.5ms（n=8），相差约 1.5 倍。测量时机器在并行编译。
69. **20 §八 两个 Miyu**：本机 auto-memory 里记的是测试时用的 `pw-link` 按名字接线，把测试音频误接进了正式 daemon 的采集口。是测试工具的问题，不是产品自己接线。
70. **20 V2 未选 B**：旧版试的是 sherpa-onnx 的 VITS（aishell3、melo，08-15，voice 分支，「CPU 音质不达标」）和 Kokoro（09-05，先定为本地兜底，「耳测后被否，代码删除」）。旧仓库和整个历史里都搜不到 espeak-ng。
71. **20 V3 未选**：会话确实是专属的（kind=voice，不进网页列表），回合走的是 daemon 普通的 StartTurn，注释写着「复用全部校验与回合机制」（`crates/miyu-hosts/src/web/voice_bridge.rs:521-543`）。
72. **20 后续再定（日语）**：样本是 MiniMax 合成的日语音色 6 条、英语音色 3 条，不是真人母语者；英语也是 0 命中（`docs/voice.md:59-66`）。
73. **20 后续再定（提示音）**：glass、marimba、softpad 三套，09-05 当天用户就耳选定稿 marimba，另两套连同 `sound_theme` 配置项一起删了（`docs/plan/2026-09-05-voice-v2.md:141-142`；`testkit/voice/sounds/synth.py:3`）。「待耳选」是定稿前的状态，只在本机 auto-memory。
74. **20 §六**：设计说「有一次」「在语音之后」。原文里 `reply.rs` 说模型「常吐」U+200B；`docs/voice.md` 把「发了语音又发正文」和「零宽空格空气泡」写成两件事。把两件连成一条因果的，只在本机 auto-memory `miyu-tts-mimo-2026-09-06.md:14`。
75. **20 §八 空设备**：进 null sink 的是 miyu-voice 自己的播放流（提示音和播报）。同一次事故还有第二个根因：两只全速 USB 麦挂在同一个 hub 上抢带宽，「喊了没反应」是这一半造成的（0fd1b11c）。
76. **20 §四 第 3 条**：公开原文只有「环境音/媒体声被转成碎片指令」一句，没写是实测，也没有样本。
77. **20 §四 第 1 条**：数字对，但省了条件：测量时机器在并行编译；测的是单跑的 test 子命令；CPU 按单核算。限 2 核时 RSS 从 74MB 掉到 10MB，原文只写了 74MB。
78. **21 §二**：「4.2 万」没写口径。它等于自有 JS 加 CSS（42,442）；算上 index.html 是 43,243；点名的三个文件合计 28,852。
79. **21 §四「详情一圈细边框」**：只有 024296e8 那一版是这样，当天 c4b9496a 就去掉了描边；HEAD 是无边框加浅色底，块之间 1px 分隔线（`web/styles.css:10970-10977`）。
80. **21 §四「六四开」「约 0.4 秒」**：次要色实际是正文色 62%（`web/styles.css:10626`）；过程区展开 0.38 秒，工具详情展开 0.32 秒（:10957）。
81. **21 §五**：与原文相反。旧版的记账、表情包仪表盘是网页自带的文件，编进二进制，靠 4 处手工注册，而且「不走插件 trait 钩子」（`crates/miyu-hosts/src/web/dashboards/mod.rs:1-5`；`docs/plan/2026-09-04-dashboard-design.md:254`）。「插件自带页面」是 AstrBot 的做法（`docs/plan/2026-09-03-plugin-dashboard-and-io.md:13`）。
82. **21 §七、X6 未选**：「本机不设密码」只在 09-11 以前成立。09-11 起网页一定要登录，建管理员账号之前用内置账号 miyu/miyu（c78022ae、3529d062、`crates/miyu-hosts/src/web/server.rs:136-141`）。09-11 以前默认监听所有网卡，不只是本机（`web/README.md:9`）。「沙盒里的命令也能冒充你」没有出处。
83. **21 §七、§八 第 4 行**：原报错是「blocked by CORS policy…access the `loopback` address space」，指向跨域策略，不是内容安全策略（`docs/plan/2026-09-11-artifact-delivery.md:268-272`）。原文的坑是「把内容安全策略的来源写对也没用」。被拦的是沙盒里读本机 `/vendor/` 的库，只在 Chromium 上出现。
84. **21 §八 第 1 行**：`container-type` 挂在外层 `.main-stage` 上，被清零的是里面的 `.chat-scroll`，不是给滚动容器本身设的；触发条件是流式输出时后代被整块替换。实测用的是 WebKitGTK 2.52，没有真机 Safari 的测量记录（`docs/plan/2026-09-10-four-items.md:26`）。
85. **21 §八 第 2 行**：原文是一次删除以后，所有开着那个会话的页面各建一个新会话，不是两个标签页同时删（d9e087ef）。
86. **22 §七 O6**：两处细节不同。多行：旧版多行不是直接交给她，首词是真命令就执行（time、test、date、which、type、command、history 除外），否则交给 `miyu --shell-classify` 判，判成命令照样执行（〔B〕`shell/fish.rs:257-287`；`src/cli/shell_bridge.rs:168-176`）。Ctrl+V：只有图片和视频变成 `[Image N: 名]`、`[Video N: 名]`；别的文件粘出来是路径原文；剪贴板里的图片数据用 8 位短名，不是原文件名（`src/cli/shell_bridge.rs:76-120`；`src/cli/repl/placeholder.rs:110-116`）。
