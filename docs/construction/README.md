## 施工方案

状态：已定（2026-09-26 项目主人批准）。M4 以后到了跟前再拆。

项目主人 2026-09-26：「我希望我们能慢慢做，细细地做。如果做的不好可以 discard 推倒重来的那种细度。」「拆分后一步步我们商量着来。」

这一份是总的施工方案：施工的规矩、总工程表、近期每一步做什么。每一步动手之前，另写一张施工单（第四节的格式），商量定了再做。

### 一、施工的规矩

1. **一步一张施工单。** 动手之前先写：这一步做什么、不做什么、要拍板的、怎么验收。商量定了再动手。
2. **一步一个工作树，推倒重来只要删掉它。** 每一步从 main 开一个分支 `step/<编号>-<名字>`，检出到单独的目录 `.worktrees/<编号>-<名字>/`，施工和验收都在那里。分支上的提交是草稿，推到 GitHub 上跑三平台 CI，三个平台全绿是验收的一部分。验收通过了才整理成正式的提交：这一步的改动合成一个提交，说明写 `施工 <编号>：<名字>`。开工前，施工单和它补的图纸先提交进 main，标题写改了哪份图纸、改了什么，不写「施工单」（2026-09-26 项目主人）；顺手做的无关改动（例如改规矩）单独成提交。每个提交都能编译、门禁全过，一起快进合进 main、推送。不满意就删掉那个目录和分支，本地的和 GitHub 上的都删，main 一个字节都不动。
3. **每一步做完，仓库都是绿的**：能编译，测试和门禁全过。
4. **一步的大小，一次坐下来看得完。** 只做一件事，新代码一般在几百行以内。
5. **验收落在测试上。** 每一步先写证明它做对了的测试；要人眼看的东西（例如事件写成 JSON 是什么样子）存成样本文件，验收时直接看。
6. **分工照旧**：技术细节按推荐定，写进施工单留痕；产品、体验、署名这类问题问项目主人，附推荐。
7. **照图纸施工。** 每一张施工单写明照的是哪几张图纸（`00-设计理念.md` 第四节「设计有图纸」）。图纸没画到的细节，先补进图纸，跟施工单一起拍板再动手。施工中发现设计有问题，先停下来改图纸，再接着做。代码不悄悄偏离图纸。
8. **不为以后写代码。** 这一步用不到的类型、字段、配置项都不写。以后要加，格式演进的规则允许加（`03-事件模型.md` 第八节）。
9. **施工图一步拼一块。** `施工图.html` 里每一步有一张图，画出这一步做完以后的样子，新加的标出来；最上面的「拼到哪了」是整张架构图：没拼的虚着，拼上的画实，块上标着是哪一步拼的。一步合进 main 时，它的那一块跟着定稿，标题栏写上提交号（2026-09-26 项目主人：「我们可以一步步做，一个个拼，很有趣」）。

### 二、总工程表

```mermaid
flowchart LR
    m0["M0 准备"] --> m1["M1 日志与投影可信"] --> m2["M2 回合可信"] --> m3["M3 第一次真对话"]
    m3 --> m4["M4 能干活"] --> m5["M5 沙盒"] --> m6["M6 长会话"] --> m7["M7 分身"] --> m8["M8 有界面"]
    m8 --> later["之后：配置与多用户、QQ、网页、语音、记忆、知识库"]
```

| 里程碑 | 做成什么样 | 主要内容 |
|---|---|---|
| M0 准备 | 仓库有了规矩和门禁 | 骨架、门禁、三平台 CI |
| M1 日志与投影可信 | 同样的日志一定投影出逐字节相同的请求，由探针守住 | 日志和投影要用的事件种类（其余的随产生它们的那一步）、追加时的检查、有效历史、统一的请求、默认的请求组装、事实注入、请求形状探针 |
| M2 回合可信 | 一轮对话的各种走法都在内存里跑通：调工具、打断、中途来消息、切权限、崩溃恢复 | 回合状态机 |
| M3 第一次真对话 | `miyu ask` 跟真模型说上话，日志落盘，缓存命中有实测数字 | 存储、第一个驱动、核心进程、协议端点、`miyu ask` |
| M4 能干活 | 能读、写、改文件，能搜索，能执行命令 | 基础系统的文件和命令工具、路径边界 |
| M5 沙盒 | 三个平台的命令都关在沙盒里 | 权限级别与沙盒 |
| M6 长会话 | 上下文满了会压缩，压完不失忆 | 压缩 |
| M7 分身 | 子代理、后台命令、给子代理留言 | 子会话与后台任务，连同 `child.spawned`、`child.reported`、`job.reported` 三种事件 |
| M8 有界面 | 能用的终端界面 | 先画界面的版式图纸，再做视图投影、终端界面；配一个看样子用的测试程序，用假数据和真的渲染代码把各种状态画出来（照 niri 的 visual tests） |

- M0 到 M3 在第三节拆到每一步。M4 以后到了跟前再拆。
- 英文版的 README：先写中文，做完以后再翻译成英文（2026-09-26 项目主人定）。
- 仓库：GitHub 上的 `miyu-agent-remake` 是公开的临时仓库。重制版能替换旧版以后，并进 `miyu-agent`：把这里的历史推进去，旧代码留在一个分支或标签上，再删掉临时仓库，星标、issue、链接都留在 `miyu-agent`。所以仓库名不写进代码和文档（2026-09-26 项目主人定）。
- 先后的理由：内核是纯逻辑，不碰 I/O（`02-内核.md` K1），所以 M1、M2 全在内存里测，不需要真模型，也不需要磁盘。M3 才第一次接上真实世界。

### 三、近期工程：M0 到 M3

**M0 准备**

| 编号 | 名字 | 做什么 |
|---|---|---|
| 0-1 | 仓库骨架 | Cargo 工作区、许可证、README、给 AI 看的 AGENTS.md、钉住工具链、第一个空 crate。施工单见 `0-1-仓库骨架.md` |
| 0-2 | 门禁 | 层序表和检查（低层不许引用高层，纯逻辑的 crate 只许用白名单里的依赖）、文件行数上限。先故意违规一次，证明它拦得住。施工单见 `0-2-门禁.md` |
| 0-3 | 三平台 CI | GitHub Actions 在 Linux、macOS、Windows 上跑格式、lint、测试、门禁。施工单见 `0-3-三平台CI.md` |

**M1 日志与投影可信**（纯逻辑：`miyu-kernel`，以及组装请求的策略模块）

| 编号 | 名字 | 做什么 |
|---|---|---|
| 1-1 | 编号与时间 | 会话编号、序号、回合编号、命令编号、调用编号、账号、blob 哈希；毫秒精度的 UTC 时间。施工单见 `1-1-编号与时间.md` |
| 1-2 | 「谁」和内容块 | 事件的 `by`；内容块（文本、思考、图片、文件、工具调用）；读到不认识的值不报错的统一写法。施工单见 `1-2-谁和内容块.md` |
| 1-3 | 事件的外壳与编解码 | 外壳的七个字段、固定的字段顺序；按种类读 body；不认识的种类原样留着；先用 `message.user` 打通。施工单见 `1-3-事件的外壳与编解码.md` |
| 1-4 | 会话与回合的事件 | `session.*`、`turn.*`。施工单见 `1-4-会话与回合的事件.md` |
| 1-5 | 消息与工具结果的事件 | `message.assistant`、`tool.result`；补上前几步漏掉的样本文件，从这一步起每种事件都有一份。施工单见 `1-5-消息与工具结果的事件.md` |
| 1-6 | 上下文的事件 | `context.injected`、`context.compacted`，每种都有样本文件。施工单见 `1-6-上下文的事件.md` |
| 1-7 | 追加时的检查 | 只追加、序号连续；一致性检查：每个结果都对得上调用、压缩只前进、撤销不越过压缩。只记一本小账，不把事件留在内存里。施工单见 `1-7-追加时的检查.md` |
| 1-8 | 统一的请求 | 请求、消息、工具规格；规范的字节、哈希；两次请求第一处不同在哪。施工单见 `1-8-统一的请求.md` |
| 1-9 | 有效历史（上） | 从最近一次压缩算起；去掉撤销的回合。内存里只留这一段，压缩一次就丢掉更早的。施工单见 `1-9-有效历史（上）.md` |
| 1-10 | 有效历史（下） | 按每次请求实际看到的范围切段；工具结果按调用顺序；群里的闲话等到下一回合开始 |
| 1-11 | 模板与转义 | 只做字段替换的模板；不可信的字段转义，防伪造标签 |
| 1-12 | 组装请求 | 内核定义接口，策略模块实现：稳定区、各种事件的渲染、触发消息放最后；出厂的英文模板放进资源目录 |
| 1-13 | 事实注入 | 环境和状态变了才注入（`08-上下文投影.md` C10） |
| 1-14 | 请求形状探针 | 一份终端会话的假日志，完整请求逐字节存档比对；每一次请求都是上一次的前缀延伸。再加随机测试：随机生成日志，查同样的日志出同样的字节、每次请求都延伸上一次 |

**M2 回合可信**（纯逻辑：`miyu-kernel` 的回合状态机）

| 编号 | 名字 | 做什么 |
|---|---|---|
| 2-1 | 输入、动作、命令 | 三组类型；会话的骨架；每个命令都有结局（不变量 7）；同一个命令编号只生效一次（不变量 9） |
| 2-2 | 流式累积器 | 模型输出的增量拼成完整的内容块；被打断时留下收全了的部分。瞬时事件的外壳和 `model.delta` 在这一步画 |
| 2-3 | 最简单的一轮 | 发消息、开回合、注入事实、挂接点、请求、流式输出、回复、结束。每次请求记一条 `model.called`（`08-上下文投影.md` 第七节） |
| 2-4 | 调工具 | 执行前的检查、执行、结果按调用顺序、下一步；步数上限；调用编号由内核分配；工具执行中的输出 `tool.progress` |
| 2-5 | 打断 | 各个阶段的打断；补「已取消」（不变量 2）；急着插话时补「已跳过」 |
| 2-6 | 中途来的消息与回报 | 在下一步的边界进来；回合结束后接着开下一轮 |
| 2-7 | 权限级别、确认与提问 | 收紧当场生效、放宽下一步生效；等人确认、等人回答；状态变了注入事实。确认和提问的四种事件（`tool.approval_*`、`question.*`）在这一步和等人的机制一起画、一起做 |
| 2-8 | 载入、崩溃恢复、撤销 | 从日志重建（不变量 8）；撤销命令 |
| 2-9 | 场景测试 | 一个假的执行器，把一整轮一整轮的对话在内存里跑完 |
| 2-10 | 随机测试 | 会话的全部输入列进一个枚举，随机排列喂给状态机，每一步查九条不变量；以后加一种输入，枚举里加一项就自动测到。平时跑几百例，合进 main 前放大长跑（照 niri 的做法，2026-09-26 项目主人提） |

**M3 第一次真对话**（接上真实世界：磁盘、网络、进程）

| 编号 | 名字 | 做什么 |
|---|---|---|
| 3-1 | 数据根 | 三个平台的默认位置、`MIYU_HOME`、目录布局和权限 |
| 3-2 | 会话日志文件 | 按段的 JSONL；追加并同步到磁盘；打开时自检、截掉不完整的尾巴 |
| 3-3 | blob | 按哈希存；先写临时文件、同步、再改名 |
| 3-4 | 驱动接口与第一个驱动的编解码 | OpenAI 兼容的对话接口；编码、解码、错误分类都是纯函数，请求字节有固定样本 |
| 3-5 | HTTP 执行器 | 流式读取、空闲超时、按错误分类处理；重试这类状态提示 `status` |
| 3-6 | 策略快照 | 出厂的人格、预设、提示词拼成快照，按哈希存档 |
| 3-7 | 会话 actor | 把状态机接上存储和驱动：先落盘，后推送。先补画核心运行日志（相当于 dmesg）的图纸，设计里只有 `07-存储.md` 第二节的一行（2026-09-26 项目主人：「像内核日志那样拥有完整的事件日志」）。日志分级参考 niri：ERROR 一定是 bug，WARN 是坏事但可能发生，trace 不进发行版 |
| 3-8 | 协议端点 | JSON-RPC 消息、本机套接字（Windows 是命名管道）、本机令牌 |
| 3-9 | `miyu ask` | 最薄的头。M3 的验收：跟真模型连说两轮，第二轮的缓存命中照 `08-上下文投影.md` 第七节量出来 |

### 四、施工单的格式

每一张施工单都写这几节：

| 节 | 写什么 |
|---|---|
| 目的 | 这一步做成以后，多了什么 |
| 图纸 | 照的是哪几张图纸；图纸没画到、这一步要先补的细节 |
| 做什么 | 新增、改动哪些文件，各自装什么 |
| 不做什么 | 容易顺手做、但不属于这一步的 |
| 要拍板的 | 问项目主人的，每一条附推荐 |
| 我定的 | 技术细节，定了什么、为什么 |
| 验收 | 能照着做的步骤，每一步写清看到什么算通过 |
| 风险 | 可能出错的地方，怎么防 |

施工单放在这个目录里，文件名是 `<编号>-<名字>.md`。一步合进 main 时，它的施工单跟着一起提交，状态改成「已完成」。

### 五、下一张

M0 完成（0-1、0-2、0-3）。1-1 已完成。1-2 已完成。1-3 已完成。1-4 已完成。1-5 已完成。1-6 已完成。1-7 已完成。1-8 已完成。1-9 已完成。下一张是 1-10 有效历史（下），施工单待写。
