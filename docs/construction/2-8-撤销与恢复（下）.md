## 施工单 2-8：撤销与恢复（下）

状态：施工中。

### 目的

撤销：你选中一轮，这一轮和它以后的全撤掉，她从此看不到，就跟没说过一样。在你发下一句之前，撤错了还能恢复，撤掉的几轮原样回来，她看到的一个字节都不变。

顺带修两处：有效历史撤一轮时，漏了上一轮排着队、被这一轮接过去的几句话；有计划的重启连着 4 轮不接以后，你开口开的那一轮，计数没从头数。

### 图纸

- `02-内核.md`，这一步新画的：
  - 第六节「撤销与恢复」，六条；
  - 第四节的表：两个命令，四个原因码；
  - 第六节「载入、崩溃、重启」第 4 条补两句：你开口以后从头数；被重启打断以后撤销过的不接；
  - 第九节的表：`turn.reverted` 改一行、`turn.unreverted` 加一行；账本多记两样。
- `03-事件模型.md`：新事件 `turn.unreverted`（第三节两张表、脑图）；第七节「撤销」「有效历史」：跟着撤的消息按回合算，多一种；恢复放回原处；撤掉的先放在一边。
- `04-核心协议.md`：`session.unrevert`，两个命令各一句。
- `07-存储.md` 第七节、`08-上下文投影.md` 第四节：撤掉的留到下一轮开始；`turn.unreverted` 那一行。
- `10-自带软件.md` 第七节、`13-终端界面.md` 第六节、`17-记忆.md` 第六节：恢复时文件、键位、记忆怎么办，各记一句，到做它们时再定。
- 样本：`turn.reverted.jsonl` 加 85 号（撤掉 76 号「清构建产物」那一轮），新的 `turn.unreverted.jsonl` 是 86 号（又恢复回来）。样本和读样本的测试放在同一个施工提交里（先改样本，测试就红）。

### 做什么

| 文件 | 装什么 |
|---|---|
| `crates/miyu-kernel/src/event/turn.rs`、`event.rs` | 新事件 `turn.unreverted`，和 `turn.reverted` 一样只有 `turns`；种类表加一行 |
| `crates/miyu-kernel/src/ledger.rs` | 撤销：没有回合在进行；撤的是还在有效历史里的某一轮和它以后的每一轮，照先后，一轮不漏。恢复：正好是最近一次撤销的那几轮。账本记下还在有效历史里的回合、还能恢复的几次撤销，下一轮开始、压缩时清掉 |
| `crates/miyu-kernel/src/history.rs`，撤销的部分拆进 `history/undo.rs` | 跟着撤的消息按回合算：触发这几轮的；由上一轮排着的消息接着开的，上一轮结束时还排着的几句。撤掉的先放在一边；恢复时照序号放回原处；下一轮开始、压缩时丢掉 |
| `crates/miyu-kernel/src/session/revert.rs` | 两个命令：撤销（空闲时、从某一轮起往后全撤）；恢复（最近一次撤销） |
| `crates/miyu-kernel/src/session/input.rs`、`action.rs`、`session.rs` | 命令 `Revert { turn }`、`Unrevert`；原因码 `turn_running`、`unknown_turn`、`compacted`、`nothing_to_unrevert` |
| `crates/miyu-kernel/src/session/load.rs` | 重启计数：不是接着干的那一轮，从头数；被重启打断以后撤销过的，不接 |
| `crates/miyu-assemble/src/render.rs` | `turn.unreverted` 不渲染 |
| `docs/designs/samples/events/` | 85 号撤销、86 号恢复 |
| 测试 | 见「验收」第 2 条 |
| 施工图 | 2-8（下）那一张 |

### 不做什么

- 撤销时把文件写回去、恢复时再改回来：要等编辑类工具报出效果（M4），到时一起问项目主人。
- 撤掉的那几轮派出去的子代理、后台命令一起停下；能恢复的时候来了回报要不要先不开回合：M7。
- 撤掉的话回到输入框、用哪个键恢复：终端界面（M8）。
- 记忆跟着撤回、跟着回来：记忆系统，和项目主人从头讨论。
- 分叉（`session.fork`）：撤不了的（压缩掉的）只回一个原因码，分叉以后做。

### 拍板的

- **你发下一句之前能恢复**：项目主人 2026-09-26 选「要，发下一句之前能恢复」，备注：「也就是undo掉undo这个操作？我觉得是可以的，因为有的时候会错误地undo掉不该undo的东西。这个反正是在拼装之前的操作，应该不难做吧？我记得opencode有这个功能，默认快捷键是ctrl+r，可以恢复undo的东西。」
- **从选中的那一轮起往后全撤**：问恢复时，两个选项的预览都写着「这句和后面的都撤掉」，项目主人选的那项照这样做。
- 先读了三家的源码（2026-09-26，codex d7b07d4、opencode 2.0.8、Claude Code 2.1.280）：
  - codex：两下 Esc 选一句，从那一轮起往后全撤，只在空闲时能撤；那句回到输入框；文件不动；没有重做。另起一份日志指向留下的那一段，她看不到撤过。
  - opencode v2：从某条消息起往后先藏起来，下一句或者压缩时才真删（`session.revert.staged|cleared|committed` 三种事件）。忙着的时候服务端拒绝，界面先打断再撤；那句回到输入框；默认连文件一起恢复（隐藏的 git 快照）。`/redo` 恢复：终端界面一次全回来，网页一次往前一句。能恢复的时候，后台任务的回报不开回合。
  - Claude Code：`/rewind` 或两下 Esc 选一句，可以只恢复对话、只恢复代码，或者都恢复；忙着时先取消；那句回到输入框；编辑类工具改过的文件有备份；没有重做；日志只追加，她看不到撤过。

### 我定的

- **有回合在进行时撤销，拒绝**：原因码 `turn_running`，头先打断再撤。三家都不在跑着的时候直接撤。
- **跟着撤的消息按回合算，不按「她听没听到」算**：试过按「这一轮之前的请求有没有看到」算，探针日志里就出错：35 号那一轮出错了、日志里没有请求的记录，撤 38 号会把 34 号「列一下 tests 目录」一起撤掉，可 45 号「换个问法」正是冲着 34 号说的。
- **由上一轮排着的消息接着开的那一轮，把上一轮结束时还排着的几句一起带走**：之前只带走触发它的最后一句，前面几句留在历史里，她就会看到没人回的话。崩了以后留下来的排着的消息不算：那一轮以 `aborted` 结束，它们排在那条结束前面，归那一轮。
- **恢复是一种新事件 `turn.unreverted`**，不去改 `turn.reverted`：撤销、恢复都是事件（03 第一节第 2 条）。英文名不用 restore：10 里「恢复」已经指撤销时把文件写回去；revert/unrevert 是 opencode 的叫法。
- **一次恢复一次撤销，从最近的往前**：照「undo掉undo这个操作」。连着撤了两次的，恢复两次。
- **下一轮开始了（不管是谁开的）、压缩过，就不能再恢复**：恢复的几轮要回到原来的位置；中间要是发过请求，她就是看着没有它们的历史说的话，放回去前后对不上。
- **账本只许撤「某一轮和它以后的全部」**：之前的账本允许单独撤中间的一轮，也允许同一轮撤两次。
- **撤销被拒分三种原因**：有回合在进行、不在有效历史里、压缩掉了（头可以提示去分叉）。序号落在最近一次压缩替代掉的范围里的，都算压缩掉了。
- **重启计数，不是接着干的那一轮从头数**：2-8（上）的测试全是连着的重启，没有「你开口」的那一轮，漏了。接着干的那一轮由被打断那一轮排着的最后一句、或者那条结束触发，照这个认。
- **被重启打断以后又撤销过的，载入时不接**：你已经动过它了。

### 验收

1. `step/2-8b-revert` 分支的 CI：三台机器全绿。
2. `cargo test`，新加的测试：
   - 撤销：撤最后一轮；从中间一轮撤，后面的一起撤；跟着撤的话（触发它的；上一轮排着、被它接过去的几句）；别处来的留着；`by`、`cause` 对；回应附上那一条的序号，落了盘才回应。
   - 撤销被拒：请求在路上、等人确认的时候，`turn_running`；没有这一轮、已经撤掉的，`unknown_turn`；压缩掉的，`compacted`（载入一份带压缩的日志来测）。
   - 恢复：恢复最近一次；连着撤两次恢复两次；恢复以后说一句，请求接着撤销前那一次往下长（`model.called` 不写第一处不同）；下一轮开始以后、没撤过的，`nothing_to_unrevert`。
   - 撤销以后说一句：请求里没有撤掉的；撤掉的那一轮里注入过的环境、权限，照 C10 重新注入。
   - 载入：撤过、恢复过的日志，载入以后接着说一句，请求和不崩一样；还能恢复的时候重启了，载入以后照样能恢复。
   - 重启计数：连着 4 轮不接以后你开口，那一轮被重启打断，接着干；被重启打断以后撤销过的，不接。这两条先写测试，退回去看它们红。
   - 账本：撤中间的一轮、同一轮撤两次、有回合在进行时撤、恢复的不是最近那次、开了回合以后恢复、压缩以后恢复，都拦下，报错说清是哪一条。
   - 有效历史：跟着撤的几句；恢复放回原处，`ordered()` 照旧；下一轮开始、压缩以后丢掉。
   - 样本：85 号、86 号读写一字不差。
   - 请求形状探针的存档一个字节都不变：规矩改了，探针里那次撤销撤的还是那几条。
   - 随机测试加上撤销、恢复：看守用自己记的排队算跟着撤的话，查请求照的是撤销、恢复以后的历史；覆盖检查加上撤销、撤销被拒、恢复、恢复被拒、恢复以后接着说。
3. 故意改坏几处，每一处都有测试变红（施工时列出）。
4. `cargo xtask check` 七项全过。

### 风险

- **有效历史改了规矩**：老的请求不能跟着变。探针、组装的两份样本请求逐字节查。
- **撤掉的先放在一边，多占内存**：最多是最近一次压缩以后的事件，下一轮开始就丢，不超过 `07-存储.md` 第七节的预算。
